# 선입금대장 (Prepaid Ledger PWA)

> ## 📘 이 리포를 처음 받았다면 → **[docs/handover/](docs/handover/README.md)**
>
> 1. **[docs/handover/README.md](docs/handover/README.md)** 를 연다 — 30분 온보딩 경로가 있다(무엇부터 읽고 무엇부터 실행하는지).
> 2. **`bash harness/verify-all.sh`** 를 돌린다 — **10/10**이 나와야 현재 상태가 정상이다(실패 로그: `/tmp/v1.log`~`/tmp/v4.log`).
> 3. 코드를 고치기 전에 **[05-invariants.md](docs/handover/05-invariants.md)**(절대 불변식 6개)와 **[04-contracts.md](docs/handover/04-contracts.md)**(깨면 안 되는 계약)를 읽는다. `index.html`을 고칠 거라면 **[08-frontend-conventions.md](docs/handover/08-frontend-conventions.md)** 도 필수다.
>
> 이 저장소는 음식점 앱 하나가 아니라 **5개 서피스**(음식점 PWA · 공공기관 담당자 웹 · 중계 서버+D1 · 문서/홈페이지 · 별도 리포의 직원용 앱)로 이루어져 있고, **`git push` 하나로 전부 배포되지 않는다** → [07-deploy-runbook.md](docs/handover/07-deploy-runbook.md).

소규모 음식점에서 단체·직원 식대 선입금을 관리하는 오프라인 지원 PWA 앱입니다.

**손님은 요청을 만들고, 사장님은 기록을 만든다** — 손님(직원)은 태블릿 잠금 화면에서 자기 잔액 조회와 사용 요청까지만 할 수 있고, 장부 기록·수정·설정은 사장님 PIN 없이는 불가능합니다.

**선금이 들어오는 두 경로** — 공공기관(구청·시청 등) 선금은 담당자(서무)가 보낸 명단이 **자동 등록**으로 들어오고, 회사(사기업)·개인 선금은 사장님이 **수동 등록**으로 직접 입력합니다. 두 경로 모두 한 장부에서 함께 관리되며, 직원마다 `소속`(선택)으로 구분합니다.

## 주요 기능
- 소속·부서별 직원 잔액 관리 (`소속`은 선택 입력 — 회사명 또는 '개인')
- 공공기관 부서 목록 관리 (전국 시도→기관 2단 선택, 직접 입력도 가능)
- 자동 등록: 공공기관 담당자(서무)가 보낸 암호화 명단 수신·승인
- 수동 등록: 한 명씩 · 빠른 등록 · 엑셀 명단(CSV, `소속` 열 선택 지원)
- 식사 사용 시 서명 기반 차감
- 손님 셀프 조회(잠금 화면 = 손님 화면): 손님이 이름 검색(초성 가능) → 본인 확인 → 내 잔액·최근 5건 확인
- 손님 사용 요청: 손님이 금액(잔액 초과 불가) 입력·서명 → [사장님 확인 받기] → 사장님이 PIN으로 열어 금액 확인·수정 후 저장해야 장부에 기록(2분 내 미확인 요청은 자동 폐기, 장부·서버에 흔적 없음)
- 일자별/직원별 거래 이력 조회
- 장부 안전 저장 (엑셀용 CSV — 잔액표에 `소속` 열 포함 + 복원용 JSON)
- 저장한 데이터 복원 (JSON)
- 4자리 PIN 잠금
- 초기 설정 마법사 4단계 (가게 정보 → 우리 가게 등록 → 공공기관 부서 목록 → 연락처·약관 동의)
- Android PWA 설치용 PNG 아이콘 제공

## 사용 방법
브라우저에서 `index.html`을 열거나, 웹 서버에 호스팅하여 사용합니다.

```bash
python -m http.server 8765
```

## 파일 구성
| 파일 | 설명 |
|------|------|
| `index.html` | 앱 전체 (HTML/CSS/JS 단일 파일) |
| `manifest.json` | PWA 매니페스트 |
| `sw.js` | 서비스 워커 (오프라인 캐시) |
| `agency-index.json` | 공공기관 인덱스 (전국 17개 시도 → 기관 2단, `schemaVersion` 2) |
| `agency-depts/{region}.json` | 시도별 기관의 과 단위 부서 목록 (선택한 시도만 lazy 로드) |
| `agency-departments.json` | 레거시 폴백 — 구형 단일 파일(서울시청·25개 구청) 과 목록 |
| `icons/` | Android PWA 설치용 PNG 아이콘 |

## 라이선스
© 2026 널마루(밥장부 · 선입금대장). All rights reserved.
열람·검증용으로 공개된 저장소이며, 사전 서면 동의 없는 복제·수정·재배포·상업적 이용을 금지합니다. 자세한 내용은 [LICENSE](LICENSE) 참조. 문의: contact@bapjangbu.com
