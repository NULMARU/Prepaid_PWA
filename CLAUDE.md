# CLAUDE.md — 밥장부(선입금대장) 프로젝트 가이드

> 소규모 음식점의 단체·직원 식대 "선입금"을 종이 장부 없이 관리하는 무료 도구.
> 기관 담당자가 직원 명단을 브라우저에서 암호화해 보내고, 음식점 폰에서만 열린다.
> ⚠️ 이 리포는 public이다. 사업 전략·운영 민감 문서는 리포에 커밋하지 않는다(.gitignore 참조).

## 라이브 서비스 지도 (2026-07 기준)

| 대상 | 주소 | 호스팅 | 배포 방법 |
|---|---|---|---|
| 소개 홈페이지 | https://bapjangbu.com | Cloudflare Pages `bapjangbu-home` | `npx wrangler pages deploy homepage --project-name=bapjangbu-home --branch=main` |
| 음식점 앱(PWA) | https://app.bapjangbu.com | GitHub Pages(커스텀 도메인) | `git push` → Actions 자동 |
| 기관 담당자 웹 | https://agency.bapjangbu.com | Cloudflare Pages `prepaid-agency` | `npx wrangler pages deploy agency-web --project-name=prepaid-agency` |
| 문서(매뉴얼·방침·약관·관리자) | https://app.bapjangbu.com/docs/… | GitHub Pages(리포 docs/) | `git push` |
| 중계 서버 | https://prepaid-relay.sulsul-plus.workers.dev | Cloudflare Workers + D1 | `cd server && npx wrangler deploy` |

옛 주소(`nulmaru.github.io/Prepaid_PWA`, `prepaid-agency.pages.dev`)는 계속 작동(301/병행 서빙).

## 아키텍처 한 장

- `index.html` — 음식점 주인 PWA(단일 파일, 오프라인 우선, IndexedDB). 원장의 진실은 이 기기.
- `agency-web/index.html` — 기관 담당자 웹(단일 파일). 명단을 음식점 RSA 공개키로 브라우저 암호화.
- `server/src/worker.js` — zero-knowledge 중계(암호문·총액·해시만 저장). D1 스키마 `server/schema.sql`.
- 전달 2모드: 「바로 보내기」(서버 경유, 수령 즉시 삭제·미수령 72h 만료) / 「직접 전달」(서버 무보관 — 파일·QR·이메일·카톡 오픈채팅).
- 인증: 기관=이메일 OTP(Resend, `noreply@bapjangbu.com`) + `X-Agency-Token` 필수(`REQUIRE_AGENCY_AUTH=1`). 음식점=RSA 챌린지-응답 소유증명(approve/deregister/재등록/원장백업/연락처).
- 운영자: `docs/admin.html`(ADMIN_TOKEN secret, 비식별 집계+의견 표), `/api/feedback`.

## 절대 불변식 (깨는 PR·기능 금지)

1. 운영자는 자금을 수수·보관·이체·정산하지 않는다(결제 기능 금지).
2. 선금 기록은 해당 음식점 1곳에서만 유효(다점포 통용 잔액 금지 — 규제 지위가 바뀜).
3. 서버는 평문 개인정보를 저장하지 않는다(zero-knowledge). 직원명·개인금액·전화번호·이메일 평문 저장 금지.
4. 전화번호는 어떤 서버 경로에도 실리지 않는다(직원 전화는 기기 로컬 AES 암호화, 클라우드 백업에서도 제외).
5. 원장의 진실은 음식점 기기다(서버는 중계·암호화 백업 보관만).
6. 완전 무료 운영(광고·유료 기능 금지). Cloudflare Workers 무료 플랜 유지(유료 업그레이드 금지 — 이메일은 Resend 무료로 해결).

## 검증·개발 명령

```bash
bash harness/verify-all.sh          # 원터치 전체 검증(로컬 e2e + 목 + 반응형 + 라이브 + 3앱 접속) — 10/10 기대
node harness/prepaid.e2e.js         # 음식점 앱 Playwright e2e
node harness/phase2.e2e.mjs         # 서버 인메모리 목 하니스(200+ 검증)
node harness/responsive.e2e.mjs     # 폰 반응형 기하 검증(360/390/412/768, 가로스크롤·잘림·모달 버튼)
node harness/phase2.live.mjs <URL>  # 배포 서버 스모크(인증 강제 검증 포함)
```

## 운영 함정·주의 (재발 방지 노하우)

- **D1 마이그레이션**: `wrangler d1 execute --remote --file`은 OAuth 토큰과 import API 비호환(오류 2036) → **`--command`로 문 단위 실행**할 것. 마이그레이션 파일은 `server/migrations-2026-07.sql`(append-only).
- **GitHub Pages 배포 실패 시**: 실패한 런을 rerun하면 중복 아티팩트 오류 → **rerun 금지, `gh workflow run pages.yml --ref main`으로 새 런**. 배포 성공 판정은 상태가 아니라 **라이브 콘텐츠 문자열**로 확인(verify-all의 앱 체크는 버전 문자열만 봐서 구버전에서도 통과함).
- **secret 등록**: `npx wrangler secret put <이름>` 실행 후 **프롬프트에** 값 입력(명령줄에 값 쓰면 이름으로 등록+노출됨). 현재 secrets: `PUBLIC_API_KEY`, `RESEND_API_KEY`, `ADMIN_TOKEN`.
- **CORS**: `server/wrangler.toml`의 `ALLOW_ORIGIN` 콤마 목록. 새 프론트 도메인 추가 시 여기 먼저 넣고 deploy.
- **공공데이터 API가 한글 조건을 못 받는다(2026-08-08~, 상대 서비스 회귀)** — data.go.kr 일반음식점 API는 `cond[…]` 값에 **한글이 들어가면 무조건 0건**(resultCode 정상 + totalCount 0)을 준다. ASCII 값이면 정상이므로 우리 인코딩·키·쿼터 문제가 아니다. 그래서 **상호 검색은 서버가 ASCII 키(우편번호 `zip`·지역코드 `region`)로 후보를 받아 우리 코드에서 걸러 낸다**(`/api/restaurants?zip=05021&q=도쿄`). 가게 실존 대조도 이름 검색이 아니라 **관리번호 `cond[MNG_NO::EQ]`**(ASCII)로 한다. 상세·재확인 절차는 `server/PROTOCOL.md §7.4`. 상대가 고치면 `?q=김밥`이 다시 0건이 아니게 되고, 코드 변경 없이 예전 경로가 살아난다.
- **`server/src/worker.js`에 실제 NUL 문자를 넣지 말 것** — 메모리 store의 키 구분자를 리터럴 NUL로 쓰면 grep이 파일을 바이너리로 보고 아무 것도 못 찾는다(진단이 크게 느려짐). `'\u0000'` 이스케이프로 쓸 것.
- **bapjangbu.com은 Cloudflare Email Obfuscation ON** — 라이브 소스에서 이메일이 `data-cfemail`로 난독화됨(정상, grep으로 이메일 검색하면 0건 나옴).
- **이메일**: 발신=Resend(`send.` 서브도메인 DKIM/SPF), 수신=Cloudflare Email Routing(`contact@bapjangbu.com` → 운영자 편지함). apex MX는 라우팅용 — Resend와 별개이니 건드리지 말 것.
- **agency-web와 homepage는 git push로 배포되지 않는다** — wrangler pages deploy를 각각 실행해야 라이브 반영.
- **Pages 배포는 반드시 `--branch=main`을 붙일 것** — 생략하면 wrangler가 **현재 git 브랜치명**을 Pages 브랜치로 써서 프리뷰 배포가 되고 커스텀 도메인(agency./bapjangbu.com)은 그대로 남는다(배포 성공 메시지는 똑같이 나오므로 속기 쉽다). 확인은 프로덕션 도메인의 **콘텐츠 문자열**로. 커스텀 도메인 반영에 수십 초~1분 지연이 있을 수 있다.
- 클라이언트-서버 계약 변경 시(예: 인증 필수화) **마이그레이션→서버 deploy→프론트 배포 순서** 지킬 것. 순서가 어긋나면 라이브 승인/제출이 일시 깨진다.

## 현재 상태 (2026-07-27)

- **필드테스트 직전 단계.** 전 기능 라이브: 이메일 OTP 인증 필수, 2모드 전달, 직원별 문자 동의·sms 자동 오픈, 약관·개인정보 동의 게이트(일회성), 클라우드 원장 백업(암호화·삭제 가능), 운영자 통계·의견, 도메인 통일(bapjangbu.com/app./agency. + noreply@/contact@).
- **개인정보처리방침·이용약관: 확정본 적용 완료**(`docs/privacy.html`·`docs/terms.html`, 시행 2026-07-06). 검토가 끝났으므로 문서·안내서에서 **'초안'·'법률 검토 후 확정' 표현은 쓰지 않는다**. 모든 서피스(음식점 앱·담당자 웹·홈페이지·안내서 3종)가 확정본 HTML을 링크한다 — `docs/PRIVACY_POLICY.md`·`docs/TERMS.md`(같은 내용의 md 미러)는 어디서도 링크하지 않는다.
- 남은 출시 게이트: 없음.
- verify-all 10/10(목 하니스 207 + 반응형 229). 알려진 경미 이슈 1건: 홈 검색 입력 중 간헐 `NotFoundError`(blur 핸들러 중 innerHTML 교체 — 해당 렌더만 버려지고 다음 입력에 복구, beta.13 이전부터 존재, 사용자 체감 없음) — 추후 렌더 스케줄링 개선 후보.

### 완료 (2026-08-12 — 소급 입력 날짜 칸(실사용일), beta.42)

- **필드테스트 보고(명풍우동)**: 수기 대장 8월분을 앱에 옮겨 적는데 날짜가 오늘로 고정. → 사용 등록·충전·
  잔액 조정 3모달에 **날짜 칸**(기본 오늘, `max=오늘` — 미래 불가) 추가.
- **설계 원칙: createdAt(원장 시계·체인 순서·잔액 사슬)은 절대 백데이트하지 않는다** — 과거로 넣으면
  reconcileEmployee 전후잔액 사슬이 어긋나 crossOk=false → 증표·손님 화면이 "잔액 표시 불가"가 되고
  백업 리마인더·월간 백업 판정도 오판(사전 조사로 확인). 대신 **`occurredAt`(발생일, 그 날 정오 고정)**
  별도 필드 + **note에 '실사용일/실거래일 YYYY-MM-DD' 자동 병기**(note는 해시 canonical 안 — 날짜도
  위·변조 방지). 오늘 날짜면 ts=0으로 기존 동작과 100% 동일, 기존 데이터 무영향(스키마 추가 필드만).
- 표시 계층만 발생일 기준: `txDispTime`/`sortTxDispDesc`/`dtDisp` — 이력 날짜 묶음·직원별 이력·손님
  최근 5건·오늘 사용액·CSV 일시(행 순서는 원장 순서 유지 — 잔액(후) 열 연속성). 발생일 지정 거래는
  시각 없이 날짜만 표시.
- **사용 모달은 비고·날짜를 2열 그리드로 압축** — 날짜 칸이 서명판을 폰 화면 밖으로 밀어 e2e 서명이
  실패했음(실사용에서도 스크롤 강요). **교훈: 폰 세로 모달에 필드를 추가할 때는 높이 예산부터 —
  서명판이 있는 모달은 특히.** e2e는 기존 5000원 차감을 지난 날짜로 저장하게 통합(별도 거래를 만들면
  'open,use' 정확 일치 데이터셋 단언이 깨짐 — 기존 데이터셋 위에 시나리오를 얹을 것).
- 하니스 계약 4종: occurredAt 저장·note 병기·createdAt 꼬리 불변·이력 발생일 그룹 + 증표 무경고.
  안내서 4장 팁 추가. verify-all 12/12.

### 완료 (2026-08-11 — 등록 메뉴 재배치·CSV 연속 업로드·4단계 간결화, beta.40 + 담당자 웹)

- **음식점 앱 설정 메뉴 재배치(사용자 지시)**: ① [① 신청 확인하기] → **[온라인 자동 등록]** — 새 명단 0건이면
  **흐린 모습(btn-idle)이되 탭은 가능**(탭하면 서버 재확인, "완전 disabled 금지" — 폴링이 놓친 신청의 탈출구),
  N건이면 btn-primary + 숫자 배지(제목 📩 배지·홈 칩 불변). ② [담당자 전달파일 열기]·[QR로 받기]를 자동 등록
  카드에서 **수동 등록 카드 하단**("공공기관 담당자에게 직접 받기" 소제목)으로 이동, ③ 카드 제목
  "수동 등록 — 직접 입력하기" → **"수동 등록"**. ①② 번호 접두 폐기. 매뉴얼·하니스 라벨 동기 갱신.
- **담당자 웹 CSV 연속 업로드 보완(실사용 보고 — 가게별 파일을 따로 만들어 보냄)**: 양식 다운로드 예시를
  음식점 2곳·3행으로(1곳 1행이 "가게마다 파일 따로"를 유도하던 원인), 3단계에 "여러 음식점이어도 파일은
  하나" 안내. **연속 업로드 = confirm으로 [확인]추가(합치기)/[취소]새로 시작** — 합친 뒤 중복은 기존
  ERR_DUP_ROW가 잡음. **깨진 두 번째 파일이 정상이던 첫 명단을 날리던 결함 수정**(거부 파일은 경고만,
  기존 명단 유지), **같은 파일 재선택 시 change 미발화(무반응) 수정**(파싱 직후 input.value 비움).
- **담당자 웹 4단계 간결화(사용자 지시)**: 하단 색 배경 설명 박스(3줄) 삭제 → **"총 합계 N원 · 음식점
  N곳 · N명" 한 줄**만 유지. step-help·skip 카드·가게별 결제 금액 문구 축약.
- **확인 결과(코드 무변경)**: 사장님이 **앱만 삭제하면 서버 등록이 남아** 담당자 웹·직원용 앱 목록에 계속
  보인다. 지워지는 경로는 자동 등록 중단 / 완전 초기화(온라인) / 운영자 수동 삭제(런북 §12)뿐. 자동
  만료는 가게 가로채기 경로라 금기(런북 §12 명시) — 폐업·이탈 정리는 수동 경로 유지.
- verify-all 12/12(배포 후 재확인). e2e에 카드 구조·idle/활성 버튼 단언 추가. **하니스 교훈: 설정 구조
  단언은 등록/미등록 분기별로 걸 것 — 미등록 시점엔 수신함 버튼 자체가 없다.**
- **후속(beta.41 + 전 서피스)**: "앱만 삭제 = 서버 등록 잔존" 안내를 웹 전반에 배치(사용자 지시) — 앱
  위험 작업(전체 초기화) 구역·[자동 등록 중단] 서브텍스트, 음식점 안내서 5장 콜아웃+FAQ, 홈페이지 FAQ
  ("앱을 삭제하면 가게 등록도 사라지나요?"), 기관 안내서(여러 번 보내도 미수령 = 앱 삭제 가게일 수
  있음 — 전화 확인). e2e에 위험 작업 안내문 정적 계약 추가. 그 외 stale 라벨 전수 grep — 잔존 0건.

### 완료 (2026-08-09 5차 — 0건 안내문을 지역 입력 상태 인지형으로, beta.39)

- **실사용 보고**: 지역 칸에 "광진구 구의동"을 이미 적었는데도 0건 안내가 "지역 칸에 …을 적으면"이라는
  고정 문구를 보여줌(입력 상태 미확인). → `emptySearchMsg(kind,regionText)` 신설: **지역 칸 실제 입력을
  보고 분기** — ①빈 칸: "적고 다시 검색해 주세요" ②동 없음(예: "광진구"): 그 입력을 짚어 『"광진구" 뒤에
  동(읍·면) 이름까지』 ③동네 검색이 진행·완료·오류 상태면 **빈 문자열**(dong 블록이 대신 말함 — 중복 금지).
- **해석 실패를 화면에 남김**: 동만 적은 경우 등 resolve 오류를 토스트(잠깐)→`{status:'error',msg}` 상태로
  영속 표시(사장님이 무엇을 고쳐야 하는지 계속 보임). 모달 상단 도움말도 "이름과 지역(구·군과 동)으로 검색"
  으로 교정(우편번호 우선 표현 잔재 제거).
- 하니스 25검사(빈 칸/구만/동만 각각의 안내문, 검색 후 중복 안내 부재, 중단 안내와 비겹침). 목 339·반응형
  2455·e2e ok. **교훈: 안내문이 사용자가 이미 한 일을 다시 시키면 안 된다 — 빈 상태 문구는 입력 상태를
  읽는 함수로 만들 것.**

### 완료 (2026-08-09 4차 — 동네 검색 전면 자동화: 전 구역 완주 + 진행 표시, beta.38)

- **자동 상한(18구역 후 탭) 폐지 → 동네(읍면동) 전체를 자동으로 끝까지 순회**(사용자 지시 — 실사용에서
  "18/49 구역에는 없었어요"가 맥락 단절). 근거: zipmap 실측 분포 — 동의 99%가 32구역 이하(~10초),
  최대 122구역(전국 0.1%). 배치(3병렬) 단위 조기 종료라 찾는 즉시 멈춘다.
- **진행 UI**: "동네 전체에서 찾는 중… n/N 구역 · 약 N초 남음"(실측 평균으로 추정, 5초 단위 올림,
  3구역 후부터 표시) + **[그만 찾기]**(`dong-scan-stop` — 배치 경계에서 멈춤). 중단 시 문구는 사용자
  지정 — **"검색 결과가 없습니다. (n/N 구역 확인) 남은 구역을 더 찾아보시겠습니까?"** + [계속 찾기].
  전 구역 소진 시 "모두 살펴봤지만 찾지 못했어요 → 직접 입력" 안내. 구 단위 확장은 구조적으로 불가
  (zipmap 키가 읍면동·리뿐).
- 하니스 21검사(중단→재개→소진 완주, 뒤쪽 구역 자동 발견, 진행 문구·그만 찾기 노출 — 목에 90ms 지연을
  넣어 진행 UI를 실검증). 목 339·반응형 2455·e2e ok.

### 완료 (2026-08-09 3차 — 검색 우선순위 재배치: 지역이 기본·우편번호는 보조, beta.37)

- **프로세스 확정(사용자 지시)**: ① 이름 검색(공공데이터 정상 경로가 기본 — 복구 시 그대로 1순위)
  ② 0건이면 지역(구·군+동)으로 **동네 자동 순회**(beta.36의 버튼 탭 단계 제거 — 검색 한 번으로 이어짐,
  usedZip 검색은 자동 이어가기 제외) ③ 우편번호는 "아는 경우만" 쓰는 보조 칸("우편번호(선택)", placeholder
  "아는 경우만")으로 강등·칸 순서도 지역이 앞. 빈 결과 안내문도 지역 우선으로 교체. 안내서 갱신.
- 하니스 16검사로 확장(자동 이어가기·칸 순서·(선택) 라벨·비추측 원칙 유지). 목 339·반응형 2455·e2e ok.
- 공공데이터 장애 배경 정황: 포털 "AI 기반 고도화 시범 운영"(2026-08-03~) 공지와 시점 일치(단정 불가,
  API 게이트웨이는 공지 문면에 없음). 활용지원센터(1566-0025) 신고문 작성해 사용자에게 전달.

### 완료 (2026-08-09 2차 — 동 이름 검색(B안): zipmap 순회, beta.36)

- **probe 결과 A안 기각**: `probe-emd.mjs` 실측 — 공공 API 응답에 동(법정동) 코드 필드가 **없다**(전체 필드
  덤프로 확인, 추정 cond 4종 전부 0건). 한글 조건 장애 지속. → B안 확정.
- **zipmap 정적 매핑**: 우체국 우편번호 DB(zipcode_DB.zip, 로그인 불필요) → `harness/build-zipmap.mjs` →
  `zipmap/{slug}.json`({시군구:{동명:[zip...]}}) + index.json. 전국 16시도·동 키 21,519개·총 1.0MB(시도별
  36~206KB lazy). 법정동·행정동·읍면·리 4종 키 전부 등재, 세종은 시군구=시도명. 갱신 연 1회.
  ⚠️ 압축 안 파일명이 cp949라 macOS unzip 실패 — python3 zipfile로 풀 것(내용은 UTF-8+BOM).
- **앱(beta.36)**: 가게 찾기(온보딩 1/3·설정 모달 공용)에서 **이름 검색 0건 + 지역 칸에 동 토큰**이면
  [『구의동』 동네 전체에서 찾아보기] 버튼 → zipmap으로 동의 우편번호 목록을 얻어 기존 `?zip=` 검색을
  **3구역 병렬 × 라운드 9구역, 자동 2라운드(18구역) 상한**으로 순회, 이후 [남은 N개 구역 계속 찾기].
  결과는 기존 [이 가게] 선택 흐름에 그대로 실림. **상호 검색이 기본, 동 검색은 0건일 때만 권하는 보조
  경로**(사용자 확정 원칙). **시도·시군구 비추측 원칙**: 동만 적으면 전국을 뒤지지 않고 "구·군과 동" 안내,
  동명이 여러 지역이면 선택 칩. 서버 무변경(기존 zip 경로 재사용).
- 하니스: `harness/dongsearch.e2e.mjs` 신설(14검사 — 구역 목록 준수·자동 상한·계속 찾기·비추측·이름 조건
  유지), verify-all [1]에 편입. 목의 /api/restaurants는 **실서버처럼 q 상호 필터를 재현해야 한다**(안 하면
  조기 발견 시나리오가 오염돼 자동 상한 검증이 공허해짐 — 이번에 실제로 걸림). 음식점 안내서 갱신.

### 완료 (2026-08-09 — 네이버 지도 링크 3서피스 + 동 이름 검색 probe, beta.35 + 담당자 웹)

- **네이버 지도 검색 딥링크**(`naverMapUrl` — `map.naver.com/p/search/` + 상호·주소, 스킴 고정 + encodeURIComponent):
  음식점 앱 설정>가게 정보에 "우리 가게 검색" 링크+[링크 복사](`copy-naver-link` 액션), 담당자 웹 관할 목록·이름
  검색 결과 행에 "지도" 링크. **편의 기능임을 명시** — 가게 확인(선점 방어)은 관리번호 대조·열쇠 지문이 담당,
  링크가 검증 수단으로 오인되지 않게 title로 고지. 네이버 API·키 불사용(플레이스 고유 URL 자동 취득은
  검색 API가 필요해 채택 안 함 — 정확 URL 등록은 /api/contact 확장 후보로 보류).
- **동 이름 검색(A안) probe**: `server/probe-emd.mjs` — PUBLIC_API_KEY로 응답 필드 실물 덤프 + ASCII 코드
  cond 실험(호출 10회 안팎). 동급 코드 조건이 있으면 "동 이름 → 법정동코드 정적 매핑" 경로 성립.
  **기본은 상호 검색(공공API 정상 시), 동·우편번호는 보조 경로**(사용자 확정 원칙).
- **직원용 앱(bapjangbu-staff, 커밋 f6638d2)**: 등록 모달에 **"우리 지역의 밥장부 음식점" 목록**
  (`/api/registered-list` — 공개 정보만, 탭 한 번에 restaurantId까지 등록), 검색 결과에 **「밥장부 받음」 배지**
  (목록 세션 캐시와 대조 — 추가 API 호출 없음), 행마다 네이버 "지도" 링크. **시·도 추측 금지** — 서울에만 있는
  구 이름만 서울로 확정(SEOUL_GUS), 중구·강서구는 시·도 포함 설정 안내(동명 구 오매칭 방지 원칙 준수).
  `harness/registered.e2e.mjs` 신설(14검사 — XSS·noopener·중구 비추측 포함), sw 캐시 v5. 직원 안내서 3장 갱신.
- 검증: 앱 e2e ok · 반응형 2455 · 목 339 · 직원용 registered 14 + zip 8. 전 서피스 라이브 반영 확인
  (app=beta.35 문자열·agency=naverMapUrl·staff=registered-list — 첫 버전 문자열 grep은 beta.27이 걸리는 함정 재확인).

### 완료 (2026-08-08 — 공공데이터 한글 조건 장애 대응: 우편번호 검색 경로, beta.34 + 서버 + 담당자 웹)

- **증상**: 설치 온보딩의 "우리 가게 찾기"가 **전국 0건**. 원인은 우리 코드가 아니라 data.go.kr
  일반음식점 API가 **조건값에 한글이 들어가면 0건**을 돌려주는 회귀(같은 조건에 ASCII 값이면 정상,
  UTF-8·EUC-KR·이중 인코딩 모두 동일). 진단 근거·재확인 절차는 `server/PROTOCOL.md §7.4`.
- **서버**: `/api/restaurants`에 **`zip`(도로명 우편번호 5자리)** 추가 — 우편번호로 후보를 받고(최대 3페이지)
  **상호는 서버가 정규화 부분일치로 거른다**(띄어쓰기·괄호 무시). 상호 조건은 요청에 그대로 남겨 두어
  상대가 고치면 자동 복구. 가게 실존 대조는 이름 검색 → **관리번호 `cond[MNG_NO::EQ]`(ASCII) 정확 조회**로
  교체 — 장애 중에도 **가게 선점 방어가 살아 있다**(라이브 하니스에서 건너뛰지 않고 통과). 응답에 `zip` 추가,
  전화번호 매핑 오류 수정(`TELNO` — 그동안 항상 빈 값이었다).
- **앱(beta.34)**: 온보딩·설정 가게 찾기에 **우편번호(5자리) 칸** 추가, 이름만으로 0건이면 "우편번호를 넣어
  다시" 안내. 입력값은 render 전에 상태로 흡수(전면 재렌더 규칙). **직접 입력 경로는 그대로** — 검색이 막혀도
  설치는 끝까지 진행된다.
- **담당자 웹**: '이름으로 검색'에 우편번호 칸·안내 추가. 안내서 2종에도 한 줄씩 반영.
- 하니스: 목 339(+2 우편번호 검증), 라이브 11(가게 선점 방어 복원), verify-all의 공공API 검사를
  **ASCII(우편번호) 기준**으로 교체(한글로 확인하면 살아 있어도 죽은 것으로 오판).
- **직원용 앱(별도 리포 bapjangbu-staff)도 반영 완료**(2026-08-09, 커밋 a2e9c04): 검색창에 숫자 5자리를
  넣으면 우편번호 조회(`isZipQuery` → `?zip=`), 이름 0건이면 우편번호 안내. "이 이름으로 바로 등록"
  탈출구는 그대로(우편번호를 몰라도 개인 기록에는 지장 없음). 그 리포에 `harness/zip-search.e2e.mjs`
  신설(8검사, Playwright는 본체 리포 node_modules를 참조)·sw 캐시 v4. `docs/manual-staff.html`도 갱신.

### 완료 (2026-08-05 2차 — 설정 미세 조정, beta.30)

- **부서 목록 관리 이동**: '가게 기본설정' 하위 → **직원 목록 관리 바로 아래**(직원 선금대장 등록 구역, 사용자 지시). 카드 키('depts')·기능 불변, 가게 기본설정 = 가게 정보·연락처·PIN.
- **설정 위계 시각화**: 접힘 카드 제목(하위 항목)을 섹션 헤더(주항목 20px)보다 **15% 작게(17px)** — `.fold-head .section-title` 규칙. 하니스가 카드 순서와 85% 비율을 계약으로 고정. verify-all 10/10.

### 완료 (2026-08-05 — 설정 메뉴 재편·홈 상단 개편, beta.29)

- **설정 전면 1단 아코디언**(사용자 지시 + 3결정 승인): 자동/수동 등록·직원 목록까지 전부 접힘, 섹션 헤더 3개(직원 선금대장 등록 / 장부 지키기 / **가게 기본설정**)는 항상 보임 — **2단 중첩 접힘 금지** 원칙. 그룹 재편: 장부 지키기 ⊃ {장부 안전 저장·복원·검사, 폰 분실 클라우드}, 가게 기본설정 ⊃ {가게 정보, 가게 연락처, 부서 목록, PIN}. 접힘 헤더 배지: 자동 등록 카드 제목에 **📩 새 신청 N건**(미등록이면 ⚠️ 등록 필요) — 홈 📩 칩이 주 알림, 헤더 배지는 보조.
- **홈 상단바**: [장부 저장] 상시 버튼 폐지 → **[직원 목록 관리] 바로가기**(설정 카드 자동 펼침+스크롤 점프 — 모달 아님: 단일 모달 구조라 목록 안 수정/충전/증표 모달과 중첩 불가). [검색 초기화] → **검색창 옆 ✕**(지울 것이 있을 때만). 부서 목록 관리는 홈에 넣지 않음(저빈도, 사용자 승인).
- **30일 백업 리마인더 배너**(`backupReminderDue`): 마지막 백업(로컬·클라우드 최신) 후 새 거래 + 30일 경과 시 홈 배너 → 누르면 [장부 안전 저장](export-safe) 동일 동작, ✕는 세션 한정 숨김. 무백업이면 첫 거래 시각 기준(설치 직후 조르지 않음). 월말 자동 클라우드 백업은 불변.
- **딥링크 자동 펼침**: go-register-store(홈 배너)·guide-start-agency가 enroll-auto 카드를 펼치고 연다(접힌 카드 DOM 미렌더 규약).
- 하니스: 아코디언 계약 재작성(전 카드 접힘+섹션 헤더 상시+배지), 리마인더 3경로(미충족/등장·✕/클릭 저장→자가 소멸), 상단바·✕ 기하(반응형 2455), openSettingsCard 선행 전수 보정. 매뉴얼 4·6장/카드 접기 콜아웃 갱신. verify-all 10/10.

### 완료 (2026-08-06 2차 — 다중 담당자 중복 방어·입금 대조 승인, beta.32 + 담당자 웹)

- **원칙(사용자 확정): 중복의 최종 판별자는 통장** — "실제 입금 1건 ↔ 명단 1건"을 사장님이 대조. 한 부서를 팀·개인별로 나눠 입금·전송하는 혼용이 현장 표준이라 앱은 차단 대신 판단 재료를 제공한다.
- **승인·직접 전달 확인창 재편**(`batchReviewText` 공용): 입금자명·입금 확인 금액을 맨 위에, 충전 대상은 이름·잔액 변화(현재→이후), 신규는 이름·금액을 나열(각 8명+외N). 동명이인 합쳐짐·이중 등록을 사장님이 이름 단위로 식별 가능(검토 항목 1).
- **경로 공통 중복 검사**(`receivedBatchLog` {h,at}, 검토 항목 2): 서버 승인·직접 전달 모두 등록 시 기록·수신 시 검사. 중복이면 "언제 올렸는지"를 실은 경고 후 사장님 선택(하드 차단 아님 — 예전 직접 전달의 무조건 차단은 다음 달 동일 명단까지 막던 결함, 해소). 교차 경로(앱 승인↔파일)도 잡힘. 구 키(receivedBatchHashes) 병행 유지, META_KEYS 등록.
- **items[].payer 신설**(CSV '입금자명' 열, PROTOCOL §2): 암호문 안에서만 이동(평문 summary 금지 — PII), batch_hash canonical 불변. 거래 note에 `· 입금 ○○○` 기록(감사 추적). 담당자 웹 4단계 확인 표에 '입금자명' 열.
- **담당자 웹**: PENDING dedupe 문구 교정("중복 없음"→"이미 접수되어 새로 접수되지 않음 · 다른 담당자 전송일 수 있음", 검토 항목 4), CSV 도움말에 입금자명 의미·팀별 부서명 표기 안내(검토 항목 3). 안내서 2종(승인=통장 대조·중복 경고 대응 / 입금자명·팀 표기) 반영.
- 하니스: 검토창 내용(입금자명·잔액 화살표·명단 나열)·중복 경고 3경로(동일 파일 재열기·교차 경로·거절 시 원장 불변) 검증. 목 323·반응형 2455·verify-all 9/10(실패 1건은 외부 data.go.kr 검색 일시 0건 — 코드 무관, 재확인 필요).

### 완료 (2026-08-06 — 보안 점검 조치 일괄 반영, beta.31 + 서버 + 직원용 앱)

표준 4종(OWASP Top 10·ASVS·STRIDE·NIST) 독립 조사 결과 → `docs/handover/11-security-review-2026-08.md`. 🔴6건 전부 + 🟡 다수 조치.
- **XSS 3종 차단**(전부 재현→차단 확인): CSV 오류 메시지(기관이 보낸 파일을 여는 정상 경로에서 발동하던 유일한 실경로), 서명 dataURL 3곳 + `sigD()` 전체일치 화이트리스트, **복원 시 meta 키 화이트리스트**(조작 백업으로 PIN·열쇠·가게ID 치환 가능하던 것 — 기기 값 보존).
- **가게 선점 방어**: 등록 시 **공공데이터 실존·상호 대조**(판정 불가는 가용성 우선으로 통과+`verified=0`), **열쇠 지문 `ABCD-EF12`** 앱 표시 + 담당자 웹 **지문 확인 게이트**(기관+부서 단위 서버 기록 `agency_keycheck`, 같은 부서 재확인 없음·다른 부서는 최초 1회·미확인 시 전송 차단·지문 변경 시 경고·전송 직전 TOCTOU 재검증), 신규 등록 배지, 선점 신고 경로. **진단 id 접두사 `__diag_`**(라이브 스모크용, 실존 대조 면제·담당자 목록 제외 — 실제 가게 id로 스모크 돌리면 그게 곧 선점이라서).
- **수신함 소유증명 필수화** + 응답에서 `batch_hash` 제거(오라클 차단), **총액·인원 대조 검증**(표시값과 실제 반영값 결속).
- **OTP 남용 차단**: 일일 예산(D1 카운터, 기본 80)·IP 시간당 5회. Turnstile은 후속(대시보드 작업).
- **보안 헤더**: 서버 4종 + Pages `_headers`(CSP·XFO·HSTS·Permissions-Policy) + 앱·직원용 앱 meta CSP. ⚠️ meta CSP는 `frame-ancestors` 무시 → **GitHub Pages 두 앱은 클릭재킹 미방어**(Cloudflare 프록시 전환 필요).
- **원장 무결성 v2**: 해시 canonical에 종류·서명해시·사유·잔액 포함 + **꼬리 앵커**(`meta.chainTip`/`txCount`) → 꼬리 삭제·유형 변조(use↔topup)·서명 교체·legacy 다운그레이드 **탐지됨**(이전엔 전부 미탐지). **원리상 한계: 키 없는 해시라 "앵커까지 다시 쓴 전면 재생성"은 여전히 미탐지** — 완전한 부인방지는 별도 서명 키 필요. 무결성 "대조 검증"이 항등식이라 항상 통과하던 것도 실제 검증으로 교체.
- **PIN PBKDF2 600k**(자기서술 형식, 최초 해제 시 자동 업그레이드). **deviceSecret PIN 래핑은 미채택** — 4자리 PIN은 오프라인 공격 방어 효과가 미미한데 PIN 분실 시 전화번호·키 영구 소실 위험이 커서. 대신 잠금 중 개인키 사용 금지·`extractable:false`·정직한 문구.
- 기타: djb2 폴백 제거(비보안 컨텍스트 차단 화면), SW 교차출처·오류응답 캐시 차단, 디버그 훅 e2e 한정, 담당자 웹 서버주소 고정·로그아웃·30분 자동 해제, `esc()` 홑따옴표, NUL 바이트 제거, feedback 전화번호 차단, 이메일 pepper(HMAC, `EMAIL_PEPPER` 미등록 시 기존 동작), 챌린지 상한, `/api/registered` 100개 초과 400.
- **직원용 앱**(별도 리포): 복원 XSS 3필드 차단(`sanitizeDB` 화이트리스트), SW 캐시 오염 차단(404가 앱 셸을 덮던 문제), meta CSP, 음성 인식·로컬 저장 고지.
- 검증: 목 323 · 반응형 2455 · 앱 e2e ok · **라이브 11/11** · verify-all 10/10. verify-all의 중계 서버 체크에 레이트리밋 재시도 추가.
- **미조치(보류)**: 약관·개인정보처리방침 3건(Resend 위탁·국외이전, 의견함 자유입력, 등록 가게 공개 게시) — 사용자 보류 지시. 72h 만료 통지 수단. 멱등키 분리(같은 인원·금액 재전송이 조용히 dedupe). GitHub Pages가 리포 전체를 앱 도메인에 서빙. Cloudflare 대시보드 설정(WAF·Rate Limiting Rule) 미확인.

### 완료 (2026-08-04 2차 — 재설치 등록 충돌 해소, beta.28)

- **실사용 장애 진단(명품우동)**: 재설치 후 "우리 가게 등록"이 계속 실패 + 담당자 웹 관할 목록 공백. 근본 원인 2겹 — ① 서버에 예전 열쇠의 등록이 남아 `register-key`가 401 `auth_required`로 거절(의도된 가로채기 방지), 앱은 이를 "인터넷 확인" 오답으로 안내 ② 라이브 D1 전 행이 beta.25 이전 등록분이라 district NULL(관할 목록에서 제외). 라이브 D1에서 명품우동 등록·백업 행 삭제로 복구(재등록 시 district 채워짐).
- **완전초기화-등록해제 통합**(`wipeDeregisterGate`): 설정의 전체 초기화는 열쇠가 지워지기 **전에** `/api/deregister`(소유증명)로 서버 등록+클라우드 백업을 해제하고 나서 지운다. 오프라인·해제 실패는 경고 confirm 후 사용자가 진행/중단 선택(막다른 길 금지). **PIN 분실 복구 경로(pinReset)는 일부러 해제하지 않는다** — 무PIN 화면이라 제3자 초기화→선착순 재등록 가게 가로채기·열쇠 백업 보유 사장님의 서버 원장 백업 소실 경로가 열림(하니스가 이 비대칭을 정적 계약으로 고정).
- **auth_required 전용 안내**(`REGISTER_TAKEN_MSG`): "이 가게는 이미 등록되어 있어요" + 해결 3갈래(①예전 폰에서 등록 해제 ②내 열쇠 백업 복원 ③contact@ 문의). 설정 경로(relayPickStore)·온보딩(finishSetup)의 "인터넷 확인" 후속 토스트가 안내를 덮지 않도록 `relayRegisterLastError`로 분기.
- **운영자 런북**: `docs/handover/07-deploy-runbook.md` §12 고아 등록 수동 해제(가게 공개 전화 본인 확인 → registry+ledger_backup 행 삭제, UPDATE로 키 대체 금지·자동 만료 금지) + §11 장애 표에 증상 추가.
- 하니스: 메인 e2e에 401 충돌 시나리오(안내 alert 1회·네트워크 문구 금지·토스트 겹침 금지·롤백 유지), 독립 `runWipeDeregister`(해제 실패 중단·오프라인 경고 중단·성공 완주·pinReset 비대칭 정적 계약). verify-all 10/10.

### 완료 (2026-08-04 — UX 마무리·OTP 확장·직원용 앱 리디자인·이관 문서, beta.27 + 서버)

- **음식점 앱 UX 5건**(beta.27): 잔액증표에 "사진으로 찍어 두세요" 안내(사장님·손님 양쪽, 명함 이미지 바깥), **첫 실행 환영 화면**(단계 번호 없음, "돈을 받거나 보내지 않아요" 안심 문구, `meta.welcomeSeen`), 서명 캔버스 안내(**캔버스 자식이 아닌 형제 오버레이** — dataURL 바이트 동일성으로 증명), **설정 아코디언**(자주 쓰는 2개만 펼침, 접힌 카드는 DOM 미렌더, 세션 한정), 🧾에 "증표" 라벨.
- **OTP 허용 도메인 확장**: `go.kr`·`korea.kr` + **`or.kr`·`ac.kr`**(복지관·공단·학교). 정규식이 라벨 경계를 강제해 `evilgo.kr`·`go.kr.attacker.com` 차단, 클라이언트·서버 규칙 등가성 23케이스 교차 검증. 목 하니스 247.
- **직원용 앱 디자인 전면 개편**(별도 리포, 라이브 https://staff.bapjangbu.com/): 컨셉 **"별이 빛나는 밥상"** — 고흐의 소용돌이 밤하늘 × 조선 풍속화(한지·먹선·오방색 절제), 원작 이미지 없이 CSS/SVG 오마주, 웹폰트·외부 요청 0건. 이모지→SVG 아이콘, 대비비·터치 타겟 전부 충족(개편 전 33/36 → 후 36/36), localStorage 스키마 불변.
- **이관 문서 체계 `docs/handover/` 11종**(MECE 축 = "언제 필요한가"): 개요·용어집 / 아키텍처 / 데이터 모델 / **깨면 안 되는 계약 C1~C8** / 불변식 강제 지점 / 테스트 책임 분담·**커버리지 공백 G1~G9** / 배포 런북 / **프런트엔드 규약(렌더 2대 원칙·캔버스 3중 보호)** / 미결 항목 / **코드 재점검 R1~R23**. 진실의 원본 표로 중복 제거.
- 부수 정리: `package.json` 라이선스 표기를 LICENSE(전권 유보)와 일치, CHANGELOG에 "beta.8에서 멈춤·CLAUDE.md가 원본" 경고, 직원 안내서의 이모지 라벨을 새 아이콘 UI와 정합.
- 반응형 2355검사·verify-all 10/10. **원격 제어 세션 활성**(claude.ai/code/session_01SLXKtXY395bAYnwL6boK7d, 세션명 밥장부-사무실).

### 완료 (2026-08-03 2차 — 온보딩 일원화·관할 목록 복구·CSV 소속, beta.26 + 서버)

- **온보딩 4→3단계**: 1/3이 곧 가게 **검색**(고르면 이름·주소·전화 자동 채움) — 기존 1/4 자유 입력 + 2/4 재검색의 중복 제거. 2/3 부서, 3/3 연락처·약관(동의 후 자동 서버 등록). 검색에 없거나 오프라인이면 [직접 입력할게요](**주소 필수·지역 해석 가능해야 통과**). 설정 자동 등록 카드는 등록 완료 시 중복 버튼이 사라지고 "관할 지역" 표시 + [관할 정보 보내기].
- **🔴 관할 목록이 안 뜨던 실사용 장애의 근본 원인 = 앱이 district를 안 보냄**: `relayDistrict()`가 검색으로 고른 가게의 공식 주소를 참조하지 않아(선택 결과의 addr을 저장하지 않음) payload에서 필드가 생략 → **라이브 D1의 district가 전국 0건**이었다. 수정: `meta.storeAddr`(공식 주소) 신설·양 경로 저장, `relayDistrict(addrHint)` 우선순위, **부팅 시 1회 자동 치유**(`districtSyncedAt`), 세종처럼 하위 기관이 없는 시도 폴백. **교훈: 서버 계약 테스트(phase2)만 있고 "클라이언트가 그 값을 실제로 만들어 보내는지"를 검증하는 테스트가 없어 verify-all 9/9인데도 라이브가 비어 있었다.**
- **서버 지역 매칭을 정확 일치로**(`LIKE '%시군구%'` → 정규화 후 `=`): 부산 서구↔강서구, 대구 서구↔달서구, 경기 양주시↔남양주시 **오매칭 3쌍**이 district가 채워지는 순간 표면화될 뻔했다(담당자가 남의 구 음식점에 명단을 보낼 수 있는 경로). 앞뒤·중간 공백 변형은 관용. 목 하니스 236.
- **담당자 웹 CSV 선택 열 '소속'**: 헤더 3종 인정(신 6열·기존 5열·구 기관명 6열), 빈 값은 2단계 기관 자동 채움, 그룹 키 `가게|소속|부서`, blob items에 `org` 추가하되 **batch_hash canonical(`name|dept|amount`)은 절대 불변**(바꾸면 기설치 앱 전부 수신 차단). 소속만 다른 동일 명단이 UNIQUE(restaurant_id,batch_hash)로 조용히 유실되는 경로는 전송 전 차단. 앱 수신부는 `it.org || summary.institution` 폴백.
- 반응형 2288검사. 배포 순서: **서버 → 앱 → 담당자 웹**(계약 변경 있음).

### 완료 (2026-08-03 — 손님 흐름 4단계 직행, beta.25)

- **본인 확인 [맞아요] → 곧바로 금액·서명 화면**(중간 잔액 화면 제거). 손님 절차 = 이름 검색 → 본인 확인 → 금액·서명 → [사장님 확인 받기] 4단계. 잔액·사용 후 잔액은 금액 화면 상단에 표시.
- 기존 잔액 화면은 **선택적 상세 화면**으로 강등 — compose 하단 보조 링크 [최근 사용 내역 보기]/[금액을 모르겠어요·사장님 부르기]로 접근. 왕복 시 **금액은 보존·서명은 재작성**(서명 dataURL 사본을 만들면 "요청은 휘발성" 불변식이 깨져 사본을 두지 않음 — 화면에 명시).
- 잔액 0 이하·체인 손상 손님은 compose 진입 불가(상세 화면 + [사장님 부르기]만).
- **소속 추천 '칩'**을 한 명씩 등록·빠른 등록 양쪽에 노출(현장 보고 "공공기관명이 안 보인다"의 실제 원인은 폰에서 datalist 팝업이 자동으로 안 열리는 것 — 목록 자체는 정상이었다). datalist는 문서에 하나만 렌더.
- **적대 검증이 잡은 실사용 차단 2건 수정**: ① 상세 화면 버튼 3개가 폰에서 전부 접힘 아래(초안 복귀 경로가 안 보임) → 목록만 스크롤되는 뷰포트 고정 레이아웃 ② **초안을 든 채 상세 화면에 가면 유휴가 60→30초로 줄어 금액이 소멸**(화면은 "금액은 그대로"라고 약속) → 초안 보유 시 stage 무관 60초. 그 외 [사장님 부르기]의 무경고 초안 폐기 → 확인 절차, 오류 표시 시 여유 6~15px → 오류 줄 자리 선확보로 레이아웃 이동 0·여유 36px+.
- 반응형 2043검사. **하니스 교훈: 접힘(fits) 계약을 한 화면에만 걸면 나머지 화면은 구조적으로 검증되지 않는다 — 새 화면을 만들면 fits 계약도 함께 확장할 것.**

### 완료 (2026-08-02 4차 — 요청 화면 통합·접두 검색, beta.23)

- **금액+서명 한 화면(`custStage='compose'`)**: 중간 단계 제거, 하단 [사장님 확인 받기]·[지우고 다시]·[뒤로]. 폰에서 **스크롤 없이 전부 보이도록** 압축(이름·소속 1줄, 잔액 1줄, 서명판 `clamp(120px,20svh,170px)` — dvh는 주소창 접힘마다 캔버스가 출렁여 svh 채택, 태블릿 220px). 검증 실측: iPhone 13에서 버튼이 화면 밖 174px → **전 폰 프로파일 화면 안**.
- **캔버스 3중 보호**: ① 금액 입력은 `custAmountPatch` 부분 갱신(캔버스·입력 노드 미접촉) ② `renderIsDisruptive`/`signInProgress` 게이트 ③ `render()` 입구의 `custComposeGuard` 강등 — 게이트를 빼먹은 새 경로가 생겨도 서명이 죽지 않는다. 적대 검증의 모든 파괴 시도(IME·붙여넣기·토스트·online/offline·visibilitychange·popstate·스크롤·직접 render)를 막아냄.
- **손님 검색 접두 일치**(`nameStartsWith`): '영'→영업맨만(홍룬영 제외), 초성도 접두. **사장님 홈 검색은 부분 일치 유지**(용도가 다름 — 주석 명시).
- **회전 시 서명 보존**: 획 좌표를 기억해 리사이즈/회전 때 backing store 재설정+균일 배율 재렌더(기존엔 오른쪽 12%가 기록되지 않고 압축 저장되던 결함 — beta.21에도 있었음).
- **유휴 시계를 '마지막 사용자 입력' 기준으로**: 렌더가 시계를 연장하지 못하게 해, 가드가 렌더를 막는 동안 손님 이름·잔액이 무기한 노출될 수 있던 성질 제거.
- 렌더 예외 근본 수정(사장님 홈 검색의 늦은 change가 render를 재진입시켜 `innerHTML` 교체가 통째로 실패하던 것 — 10건→0건). **하니스가 `consoleProblems`를 수집만 하고 검사하지 않던 구멍을 봉인**(이제 예상 외 오류·렌더 예외는 실패). 반응형 하니스는 `scrollIntoView` 후 측정하던 것을 **초기 뷰포트 가시성**으로 교체(1706검사).

### 완료 (2026-08-02 3차 — B단계 손님 사용 요청, beta.21)

- **B단계(손님이 금액·서명까지 작성하는 '요청')**: 잔액 화면에 [사용 요청하기](금액 입력 — 빠른금액·직접, **잔액 초과 불가** → 서명 → **[사장님 확인 받기]**)와 [사장님 부르기](대상만 인계, A단계 유지 — 금액을 사장님이 정하는 자리·잔액 초과 상담용). PIN "사장님 확인" 화면에 **"손님이 입력한 금액"** 라벨과 금액을 최대 크기로 + 서명 썸네일. 사장님 PIN 후 사용 모달에 금액·서명 프리필(금액 수정 가능), **저장은 기존 confirm 경로로만** — 자동 저장 없음, 요청은 휘발성(원장·백업·서버에 흔적 0, 2분 TTL).
- **금액 불일치 시 서명 자동 무효화**: 사장님이 금액을 손님 요청과 다르게 고쳐 저장하면 손님 서명을 폐기하고 재서명을 요구(분쟁 방어). 모든 요청 거래의 note에 `손님 요청 금액 N원` 기록.
- **독립 적대 검증에서 돈이 어긋나는 결함 2건 사전 차단**: 사장님이 고친 금액이 (a) [서명 다시 받기] (b) **네트워크 online/offline 전환**만으로 손님 요청값으로 조용히 되돌아가던 문제 — 원인은 입력값이 state에 없었던 것. `captureUsageInputs()`로 재렌더 전 흡수 + 비동기 render 경로 전수 가드. 같은 결함군으로 **토스트 표시만으로 그리던 서명·입력이 지워지던 버그**도 수정(`paintToast` 부분 갱신).
- 그 외: 서명 그리는 중 비동기 render 방어(캔버스는 포커스를 못 받아 기존 가드가 死코드였음), 모달 초기 포커스를 금액칸으로(Enter 한 번에 요청 금액이 9,000으로 덮이던 문제), `parseWon`이 0x/1e/0b/0o·전각 표기 거부, 캔버스 호버만으로 유휴 무한 연장되던 것 차단.
- 반응형 1464검사. **교훈: 전면 재렌더 앱에서 "프리필"을 도입하면, 되돌림이 빈 칸이 아니라 '그럴듯한 오답'이 되어 실패가 눈에 보이지 않게 된다 — 입력값은 반드시 state에 흡수할 것.**

### 완료 (2026-08-02 2차 — 손님 화면 모바일 입력 수정·결과 없음 안내, beta.20)

- **모바일 입력 장애 근본 수정(실사용 보고)**: 손님 검색은 글자마다 전체 innerHTML 재렌더 → 입력 노드가 매번 교체됐다. 한글 IME는 한 키 안에서 `compositionend→compositionstart→input`이 흐르므로 그 사이 노드가 사라져 **글자가 화면에 안 찍히고 폰은 소프트키보드가 닫혔다**(데스크톱은 소프트키보드가 없어 무증상). → `custSearchPatch()` 신설: 손님 검색 중에는 **결과 영역(`#custResults`)만 부분 갱신**하고 입력 노드는 유지. 조합 중에는 전체 render로 넘기지 않음. 화면 전환(결과 1명→확인 카드)일 때만 전체 render.
- **교훈**: 이 앱은 전면 재렌더 구조라 **입력이 있는 화면에 실시간 필터를 붙일 때는 부분 갱신이 필수**. 회귀 테스트는 "화면이 갱신되는가"가 아니라 **"입력 노드가 살아남는가"(노드 동일성·focusout 0회)** 를 단언해야 헤드리스에서도 검출된다(수정 전 코드로 되돌리면 141건 실패).
- 검색 0건 안내 추가: "'○○'(으)로 등록된 이름이 없어요. 사장님께 말씀해 주세요." + 초성 팁(질의 전에는 미표시).
- 반응형 하니스 1140검사(Pixel 7·iPhone 13 실디바이스 프로파일 포함), 안내서 2종 반영.

### 완료 (2026-08-02 — 사장님 확인 흐름·유휴 시계 정리, beta.19)

- 손님이 [사장님께 보여주기]를 누르면 **바로 PIN 화면("사장님 확인 / ○○○님 · 사장님이 비밀번호를 입력해 주세요")** — 별도 안내 화면 제거, 성공 시 기존 인계(사용 모달 자동 오픈) 그대로. [처음으로]로 요청 취소.
- 홈 상단 버튼 **[손님에게 넘기기] → [손님 셀프 조회]**(액션명 불변).
- **유휴 시계 3종 정리**: 손님 화면 30초 · 사장님 확인(인계) 화면 30초 · **사장님이 직접 연 비밀번호 화면 120초**(`TIMERS.ownerPinIdle`). 만료 시 입력 중 PIN 숫자까지 폐기하고 손님 화면 복귀. 독립 적대 검증이 잡은 결함 수정 — owner 화면에서 시계를 없앴더니 ① 부분 입력 PIN이 무기한 남아 해제 난이도 1/10,000→1/10 ② 손님 오탭 시 태블릿이 PIN 패드에 영구 체류(셀프 조회 정지)가 발생했다.
- `lock-to-pin`이 pending 요청도 폐기(요청은 handoff 경로에서만 유효), `handoffEmp()`에 TTL 판정 추가(만료 요청을 계속 광고하던 문제) + 만료 토스트 유지.
- 하니스 은폐 단언 교정(“owner 화면은 복귀하지 않는다”→“120초 내 복귀+입력 삭제”, 공허했던 pending 단언을 실제 상태에서 검증), 파일 input 3종 change를 잠금 스윕에 포함, 프로덕션 타이머 상수 6종 단언. 반응형 921검사.

### 완료 (2026-08-01 — 손님 셀프 조회·자동 잠금·검색 개선, beta.17~18)

- **잠금 화면 = 손님 화면(A-lite+A-full)**: 원칙 **"손님은 요청을 만들고, 사장님은 기록을 만든다"**. 무조작 90초 자동 잠금 또는 홈 [손님에게 넘기기] → 손님이 이름(초성 가능) 검색 → "기관명 부서명 + 이름"(잔액 비표시, 20건 상한) → 본인 확인 → **잔액+최근 5건**(서명은 텍스트 배지, 취소건 회색) → [사장님께 보여주기] → 사장님이 PIN 해제하면 **2분 이내 그 직원 사용 모달 자동 오픈**(금액 프리필 없음). 손님 화면 30초 자동 복귀.
- **잠금 게이트**: `onClick`/`onInput` allowlist로 잠금 중 원장 쓰기 경로 구조적 차단(파일 input 3종 포함). 복구 화면의 파괴 버튼(복원·초기화)은 **진입 후 60초 카운트다운 뒤에만 활성**, PIN 5회 실패 60초 지연은 **meta 영속**(새로고침 우회 차단).
- **동명이인 마커 재설계**: 이름 뒤 라틴 소문자(a/b) → **`·2`,`·3`**(라틴 이름 'Alex'도 정상 부여, Alex/Alec 오병합 소멸). 손님 화면은 동명이인 그룹을 병합·선택 차단.
- **원장 체인 오판 근본 수정**: 배치 거래의 무작위 소수 createdAt 제거 → `nextTxTime()` 단조 증가 정수, `verifyChain`을 시각 정렬이 아닌 **링크 추적**으로 재작성(레거시 소수·동률 백업 호환), `chainTip` 재정의, 정렬 비교자 7곳 일원화. CSV·승인 배치 후 손님 화면 잔액이 간헐 실패하던 비결정성 제거(20회 반복 검증).
- beta.17: 초성 검색·사용 모달 빠른 금액·1건 확대 카드(요약 동시 숨김)·최근 사용 순·검색창 전폭, 월간 배너→[장부 저장] 상단 버튼, '전체 부서'→'전체 소속'. 소속(선택) 추천에 등록 공공기관명 추가(그 값이면 orgKind=public).
- 검증: verify-all 10/10, 반응형 629검사, 적대 검증 라운드에서 치명 2건(3탭 전체 삭제·임의 백업 복원 탈취) 포함 8건 사전 차단.
- **미착수(승인된 다음 단계)**: B단계 — 손님이 금액·서명까지 미리 작성하는 "요청" 흐름(필드테스트 실사용 관찰 후 착수 권고).

### 완료 (2026-07-30 3차 — 그룹 제목·정렬 확정, beta.15)

- 공공기관 그룹 제목을 **항상 "기관명 부서명"**(예: 광진구청 재무과)으로 — 사장님 구분 편의(사용자 지시). 충돌 안전망은 종류 꼬리표((공공기관)/(회사)/(소속 없음))로 단순화 유지.
- 정렬(홈·설정 공통): 공공기관(기관명 가나다→부서 오름차순) → 회사(가나다) → 개인 → 무소속(라벨 가나다). 부서 필터 옵션도 동일 순서. 정렬은 표시 문구가 아닌 원본 필드 기준(`groupSortTuple`).

### 완료 (2026-07-30 2차 — 홈 계층 그룹핑, beta.14)

- **홈 그룹 아코디언**: 헤더 규칙 = 공공기관 **부서명만**(제목 충돌 시에만 "(기관명)" 병기) / 회사 = **회사명**(펼치면 부서 하위 소제목) / 개인 = "개인" / 레거시 = 결합 라벨 불변. 헤더에 인원·잔액 소계, 탭·Enter/Space로 펼침, 그룹 1개·검색·필터 시 자동 펼침(강제 펼침 중 탭은 무시). 홈 하단 요약("활성 직원 N·잔액 합계")은 설정>직원 목록 관리 상단으로 이동.
- 직원 `orgKind:'public'` 플래그(자동 등록 경로에서 세팅·**라벨 불변 가드 하에 동일 라벨 활성 직원 전체 백필**), 수정에서 소속·부서를 바꾸면 분류 재설정. 그룹 키는 JSON 직렬화(자유 입력 충돌 방지), stale 부서 필터 자기치유.
- 집중 적대 리뷰로 결함 7건(부분 백필 그룹 분열·동일 제목 중복·필터 고아화·분류 고착·키보드 접근·유령 토글·구분자 충돌)을 배포 전 수정, 회귀 테스트 편입. 매뉴얼 4장 재구성(그룹 규칙·자동 펼침·요약 이동 FAQ).

### 완료 (2026-07-30 — 소속 필드·공공기관 용어·폰 반응형·UX 총점검, 검증 완료)

- **소속(org) 필드 신설**: 직원 레코드에 소속(공공기관명·회사명·'개인') 추가 — 자동 등록 승인·직접 전달이 기관명·부서를 분리 저장(합성 중단), 표시는 결합 라벨(`orgDeptLabel`)로 기존과 동일. **매칭 원칙 = "화면에서 같아 보이는 직원 = 같은 직원"(라벨|이름 키)** — 레거시 결합 dept와 신규 분리 데이터가 섞여도 충전이 정확히 붙는다. 수동 등록에 "소속(선택)" 입력(datalist: 개인+기존 소속), CSV `소속|회사` 열 인식(단 `부서` 포함 헤더 제외 — '소속부서' 충돌 방어), 내보내기 잔액표 소속 열. 백업 schemaVersion 3 유지.
- **승인·직접 전달 = 기존 직원 충전**: 같은 소속·부서·이름 활성 직원은 신규 카드 대신 topup(매달 재전송 시 카드 증식 방지, CSV와 동일 원칙). 승인 안내 "신규 N명 · 기존 충전 M명".
- **용어**: 음식점 앱 사용자 노출 '기관' → **'공공기관'** 전수(55회), "공공기관 담당자(서무)"·"공공기관 부서 목록 관리"(공유 목록임을 키커에 명시), DEFAULT_ORG='공공기관'. 워크플로 확정: **공공기관=자동 등록, 회사(사기업)·개인=수동 등록** — 홈페이지 FAQ·안내서·전단에 명시.
- **폰 반응형**: `.stack` minmax(0,1fr)(가로 스크롤·설정 탭·모달 저장 버튼 문제 일괄 해결) + ≤640px 직원 카드 2행(이름·금액 전액 표시). `harness/responsive.e2e.mjs` 신설(기하 단언 108, verify-all 10번째 항목).
- **UX 총점검 반영 16건**: `.btn-primary.small` 흰글씨/흰배경 블로커(온보딩 "이 가게" 버튼 안 보임), 진한 버튼 위 저대비 설명·배지 깨짐, 홈·이력 빈 상태 CTA, 폰 모달 버튼 역순(저장이 맨 위), 취소 버튼 명확화, 해요체 통일, 글자 크기·터치 타겟(고령 가독), 토스트 위치 등. CSV 내보내기 잔액표가 홈 필터를 따라가던 누락 버그 수정. 앱 beta.13.
- **미적용(검토 후 결정 대상)** — ⚠️ 이 목록은 2026-07-30 시점 기록이며 **대부분 이후 해소됨**(현재 미결은 브랜드명 통일 1건뿐, `docs/handover/09-open-items.md`가 최신 진실): ~~온보딩 가게 이름 중복 입력 통합(beta.26 완료)~~, ~~0/4 환영 화면(beta.27 완료)~~, **브랜드명 통일(앱 '선입금대장' vs 홈 '밥장부' — manifest 영향) — 사용자 보류 결정**, ~~사용 모달 빠른 금액 버튼(beta.17 완료)~~, ~~서명 캔버스 안내(beta.27 완료)~~, ~~설정 화면 아코디언(beta.27 완료)~~, ~~잔액증표 🧾 라벨(beta.27 완료)~~.

### 완료 (2026-07-27 — 용어 대개편·복수 전송·알림·보안 강화, 로컬 검증 완료)

- **용어 체계 확정**: 상위 개념 **"직원 선금대장 등록"** = **자동 등록(기관에서 보낸 명단 받기)** / **수동 등록(직접 입력: 한 명씩 등록·엑셀 명단(CSV) 불러오기·빠른 등록)**. "구청/기관 선금 받기" 계열 전면 폐기(돈은 계좌 이체 — 오해 차단 문구를 앱·담당자 웹·홈페이지에 배치). 담당자 웹 배지 "✅ 명단 받기 가능", 상태칩 "기관 명단 받는 중".
- **앱(beta.12)**: 설정 최상단 "직원 선금대장 등록" 그룹(자동/수동 카드), 직원 목록 관리 분리. 온보딩 4단계(2/4 **우리 가게 등록** — "나중에 등록할게요" 상시 탈출구+간판≠인허가 상호 팁, 실제 등록은 약관 동의 후, 실패 시 롤백+홈 배너, 기존 사용자 1회성 마이그레이션). 등록 성공 직후 **내 열쇠 백업 강권**(가이드·설정 배지·홈 배너 — 폰 분실=영구 잠금 방지). 루트 manual.html 삭제(sw 캐시 포함).
- **알림**: `GET /api/inbox-count` 신설(개수만·무인증·inbox와 동일 JOIN 필터) → 앱 시작·online·visibilitychange(60s 스로틀) 시 확인, 홈 "📩 새 명단 신청 N건" 칩.
- **담당자 웹 복수 전송**: 목록 전체 선택/선택 전체 해제·선택 카운터, 진행률(음식점 N곳·부서 조합 M건), 429 백오프+요청 예산 페이싱(전역 60/분·공개키 20/분), 가게별 결과표+실패 재시도, **동명 음식점 오배송 3중 차단**, 공백 행 rid=undefined·직접전달 파일명 충돌 버그 수정, CSV 오류 '사유' 열, 토큰 sessionStorage 보존(새로고침마다 OTP 소모 방지 — Resend 절약 1순위 레버), 에러 코드 전면 한글화.
- **서버**: 담당자 이메일 **해시 저장**(방침 문구와 일치화 — 불변식 3; 라이브 구 평문 행은 10분/24h TTL 자연 소멸), OTP 발송 실패 시 스로틀 미소모+형식 검증, **submit 멱등 응답 `{deduped,status}`**(승인/거절 후 재전송 블랙홀 해소 — 담당자 웹이 3분기 표시), feedback 180일 TTL, 크론 IN-배치화.
- **문서**: 안내서 3종을 실제 구현 라벨·흐름과 전수 일치화(제3자 제공 동의 모달, 재전송 규칙, 제외 vs 체크해제 등) + A4 인쇄 CSS → **PDF 3종 `docs/manuals/`**. 홈페이지·전단·덱 용어 정합.
- **남은 권고(미적용)**: ① 약관·방침에 수동 등록(사장님=수집주체)·register-key(가게명·공개키·공개 목록 게시) 항목 반영 검토, ② `/api/inbox` 암호문 반환에 소유증명 인증 추가 검토, ③ 72h 만료 미수령을 담당자가 알 수단(보낸 건 상태 조회) 부재, ④ OTP 화이트리스트가 go.kr/korea.kr뿐(.or.kr 복지관 등 협의 필요), ⑤ staff.bapjangbu.com 전환(수동 작업), ⑥ 레이트리밋이 per-isolate 메모리(베스트 에포트).

### 완료 (2026-07-26 — PR #5 머지 + 랜딩 3주체 재구성, 전부 라이브 배포·검증)

- **PR #5(머지됨)**: 홈페이지 5단계 인포그래픽(시연 애니메이션 JS 제거), 전용 태블릿 비치 권장 문구(홈페이지·음식점 안내서·전단·협회 덱), 전체 흐름 5단계 양쪽 매뉴얼 반영, 직원용(베타) 링크, 전단·덱 주소 app.bapjangbu.com 갱신, ALLOW_ORIGIN에 staff.bapjangbu.com 선등재.
- **랜딩 3주체 재구성**: 이용 주체를 **음식점 사장님(파랑)·기관 담당자(서무)(금색)·직원(초록)** 3색으로 코딩 — 히어로 3버튼(직원용 추가), 역할 섹션 배지, 헤더 **'사용 매뉴얼'** 탭(#manuals 3장 카드)·푸터 링크. '기관 담당자' → **'기관 담당자(서무)'** 문구 통일. 흐름 아래 "실제 앱도 이만큼 쉬워요/지금 시작하기" 블록 삭제(대신 "직원은 어디에 있나요?" 한 줄 안내). 의견 폼 role에 **'직원'** 추가(서버 화이트리스트·PROTOCOL.md 반영).
- **`docs/manual-staff.html` 신설(직원 안내서)**: 직원용 앱(bapjangbu-staff) 실제 기능 기준 — 이용 지역 설정·음식점 등록(LOCALDATA 검색)·충전/사용 기록·말로 입력(자연어)·이력 수정·백업/복원. **"공식 잔액은 음식점 장부가 기준"**(직원용은 개인 기록장, 음식점 앱과 미연동)을 최상단 경고로 명시.
- 기존 매뉴얼 2종에 `charset`·`viewport` 메타 추가(모바일 확대·로컬 열람 깨짐 방지) + 안내서 3종 상호 링크.
- **비용추계 문서**: `docs/COST_MODEL.md`(gitignore, 로컬 보관) — 결론: 전국 10만 음식점에서도 월 ~₩10만. 먼저 막히는 것은 **Resend 무료 "하루 100통"**(OTP), 그다음 data.go.kr 일일 한도, D1 저장 5GB. 무료 연장 레버 1순위 = 담당자 세션 유효기간 연장.
- **OTP 발송 한도 안내(신규)**: Resend가 429(하루/월 한도)를 주면 서버가 `429 {error:'email_quota_exceeded'}`로 구분 응답 → 담당자 웹이 "오늘 인증번호 발송 한도 도달 · 잠시 후/다음 날 재시도" 안내를 띄운다(1단계 화면에 상시 고지문도 표시). 기관 안내서 1단계에도 같은 안내 + "마감 직전보다 여유 있게" 권고. 목 하니스 14c-2b로 회귀 검증.
- **기관 안내서 정리**: 9번 '문의' 섹션(담당 부서 연락처 자리표시자) 삭제, 인증번호 관련 stale 문구("파일럿 기간에는 자동 입력") → 실제 동작(기관 이메일로 발송·유효시간 10분·스팸함 확인)으로 교체.
- **담당자 웹 전송 동의 모달 문구 정리**: 파일럿 배지("⚠ 파일럿 테스트 모드 — 동의 문구는 법률 검토 후 확정됩니다")와 "※ 문구는 법률 검토(출시 게이트) 후 확정됩니다" 삭제 → 확정본 링크(개인정보처리방침·이용약관, 시행 2026-07-06) + "대상 직원 본인의 사전 동의 확보는 제공 기관의 책임" 고지로 교체. 미사용 `.pilot-badge` CSS 제거. 형식확인 fallback 문구도 "(파일럿 — 정식 이메일 인증은 준비 중)" → "이 서버에서는 인증번호 확인이 진행되지 않았습니다"로 수정(정직성 원칙 유지, PROTOCOL.md 반영).
- **직원용 앱(별도 리포 NULMARU/bapjangbu-staff, PoC)**: 라이브 **https://staff.bapjangbu.com/** (2026-08-03 도메인 전환 완료 — CNAME staff→nulmaru.github.io(DNS only) + Pages 커스텀 도메인·HTTPS 강제, CORS 선등재, 홈페이지·안내서 링크 교체). 옛 주소 `nulmaru.github.io/bapjangbu-staff/`도 계속 작동. 코드는 상대경로라 무수정. ⚠️ localStorage는 오리진 단위 — 전환 시점에 시범 사용자 0명이라 이전 비용 0이었다(이후 사용자가 생기면 백업 JSON으로만 이전 가능).

## 기관·부서 데이터 (전국)

- `agency-index.json`(시도→기관 2단, schemaVersion 2, 17개 시도·246 기관) + `agency-depts/{region}.json`(시도별 과 목록, lazy 로드). 서울은 큐레이션, 나머지는 행정표준코드 기관코드에서 자동 추출(빈 기관은 앱이 공통 템플릿 폴백).
- 재생성: `node harness/build-agencies.mjs <기관코드 전체자료.txt>` — 원자료는 code.go.kr 기관코드 조회자료 다운로드. 연 1회(조직개편 후 2~3월) 갱신. 상세: `docs/agency-data-pipeline.md`.
- 주소→기관 자동 매칭은 시도 우선 2단계 — 동명 구(중구 등)는 시도 없이 매칭하지 않는다(오매칭 방지). 수정 시 6개 광역시 "중구" 케이스 필수 테스트.

## 문서 위치

- 사용법: `docs/manual-restaurant.html`(음식점), `docs/manual-agency.html`(기관)
- 홍보물: `docs/marketing/`(전단·기관 안내문·협회 덱, 인쇄용)
- 프로토콜·API 계약: `server/PROTOCOL.md`
- 사업·운영 민감 문서: 리포 미포함(로컬 보관, .gitignore 등재) — 커밋 금지 유지
