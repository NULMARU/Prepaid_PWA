# 변경 이력 (CHANGELOG)

이 문서는 선입금대장 PWA의 주요 변경 사항을 기록합니다. 버전은 앱의 `APP_VERSION`
(= `sw.js`의 캐시 이름)을 기준으로 하며, 날짜는 해당 버전을 도입한 커밋 날짜입니다.

> 📌 **읽는 순서** — 이 파일은 릴리스 단위 요약입니다. 각 릴리스의 배경·설계 판단·
> 하니스 계약까지 담긴 상세 기록은 루트 [`CLAUDE.md`](CLAUDE.md)의 "완료" 절에 있고,
> 코드를 이관받는 개발자는 [`docs/handover/`](docs/handover/README.md)부터 읽으십시오.
>
> 결번: **beta.11·22·24**는 배포되지 않은 번호입니다. **beta.44**는 별도 커밋 없이
> beta.45 커밋에 함께 실렸습니다(아래 항목 참조).

## [1.0.0-beta.45] - 2026-08-25

현장 테스트에서 "설정 화면의 용어·구성·순서에서 막힌다"는 보고를 받아 설정 화면을
전면 간소화하고, 첫 홍보물 세트를 만들었습니다.

### 변경 (Changed)
- **설정 카드 배열 원칙 확정**: 카드는 위에서부터 `오늘 할 일 → 참고 → 되돌리는 일(위험)`,
  카드 안의 진한 버튼(`btn-primary`)은 하나만, 빈 상태는 값처럼 보이지 않게 "아직 고르지 않음".
- **자동 등록 카드**: 문구를 "우리 가게 중복 등록을 막는"으로 교체, 교차 안내 2곳 삭제,
  순서 재배치(등록된 가게 → 온라인 자동 등록 → 열쇠 지문 → 관할 지역 → 자동 등록 중단).
  미등록 상태에서는 열쇠 지문·열쇠 백업을 표시하지 않습니다(첫 버튼은 [우리 가게 등록] 하나).
- **열쇠 백업 위치 이동**: 지문의 [복사] 버튼을 [내 열쇠 백업(중요)]으로 교체하고 장부 지키기
  그룹에서는 제거(메뉴 중복 금지). 접힌 카드 요약의 `⚠️ 내 열쇠 백업 아직 안 함` 배지와
  버튼 배지가 같은 사실을 말하도록 하니스가 동치를 고정합니다.
- 설정 화면 한정으로 '비밀번호' → **'화면잠금 비밀번호'**(바로 위 '열쇠 지문'과의 혼동 해소).
- **수동 등록 카드 순서**: 엑셀 명단(CSV) → 공공기관 담당자에게 전달받기 → 한 명씩 등록 →
  빠른 등록. 「담당자 전달파일 열기」를 **「담당자가 카톡·메일로 보낸 명단 파일 열기」**로 개명.
- **소속(org) 안내 문구 통일**: 세 자리 모두 "공공기관명·회사명, 개인 선금이면 '개인'"으로.
  CSV는 `소속·부서·이름·금액` 4칸을 명시하되 소속은 선택 유지(기존 파일 호환).

### 추가 (Added)
- **홍보물 세트**(`docs/marketing/`): 일반 대중용 A4 1p, 공공기관 인트라넷 게시용 A4 1p,
  SNS 카드뉴스 6장(1080×1080) + OG 배너(1200×630), 게시 문구 모음. QR은 내장
  qrcode-generator(MIT)로 **정적 SVG 생성**(외부 요청 0건, BarcodeDetector 디코드 검증 완료).

### 수정 (Fixed)
- **공공기관 부서 목록 관리의 '첫 항목' 폴백 제거**: 고른 적이 없으면 `null`이며, 화면은
  ①시·도 ②거래 공공기관 ③과 목록 3단계로 하나씩 열립니다. 이전 폴백은 고르지도 않은
  기관명이 체크 한 번에 `meta.orgName`으로 저장될 수 있는 경로였습니다.
- 고급 설정의 중계 서버 주소 입력칸 제거(CSP가 기본값 외 도메인을 막아 바꾸면 오히려 고장).
  기본값이 아닌 기기에는 [기본값으로 되돌리기] 한 줄을 노출합니다.

## [1.0.0-beta.44] - 2026-08-23

> 별도 커밋 없이 beta.45 커밋(`ab919b5`)에 함께 실렸습니다.

### 변경 (Changed)
- **손님 금액·서명 화면**: 네 조각(안내문·입력 금액·현재 잔액·예상 잔액)을 **"잔액 N원이
  남아있어요" 한 줄**로 합치고 화면에서 가장 큰 글자로 키웠습니다. 이 줄은 **현재 잔액 고정**
  — 입력 금액을 반영해 줄어들면 손님이 "이미 빠져나갔다"로 읽습니다(실제 차감은 사장님 저장 1회).
- 서명 안내 문구 추가: "여기에 서명하시고, 사장님 확인받기를 클릭해주세요".
- **사장님 사용 등록 모달 정리**: 빠른 금액 3버튼 삭제(`fill-use` 액션까지 제거 — 손님이 서명한
  금액을 오탭으로 덮어쓰는 경로), [서명 후 저장] → **[저장]**, 화면상 버튼 순서를
  저장 → 서명 다시 받기 → 취소로.
- **저장 직후 '증표를 보여줄까요' 확인창 삭제 → 증표 화면 직행**(모든 사용 저장).

### 수정 (Fixed)
- `#custAmtBig`·`#custAmtAfter` 제거에 맞춰 `custAmountPatch`의 필수 노드 목록도 축소.
  하나라도 못 찾으면 전체 render로 떨어져 그리던 서명이 사라집니다(3중 보호 계약의 첫 겹).

## [1.0.0-beta.43] - 2026-08-12

### 변경 (Changed)
- 손님이 요청한 금액을 **그대로** 저장할 때 중복 확인창을 제거했습니다 — 사장님은 PIN 화면에서
  그 금액을 최대 크기로 이미 확인했습니다. 확인창은 사장님 직접 입력·요청 금액 수정·지난 날짜
  소급 입력에서만 유지됩니다.

## [1.0.0-beta.42] - 2026-08-12

### 추가 (Added)
- **사용·충전·조정에 날짜(실사용일) 칸**: 수기 대장을 소급 입력할 수 있습니다. 발생일은
  `occurredAt` 별도 필드에 담고 `note`에 "실사용일 YYYY-MM-DD"를 병기해 해시로 봉인합니다.
- 이력 날짜 묶음·손님 최근 5건·오늘 사용액·CSV 일시를 발생일 기준으로 표시합니다.

### 변경 (Changed)
- `createdAt`(원장 시계·해시체인·잔액 사슬)은 불변 — 기존 데이터에 영향이 없습니다.
- 사용 모달의 비고·날짜를 2열로 압축해 서명판이 한 화면에 남도록 했습니다.

## [1.0.0-beta.41] - 2026-08-11

### 추가 (Added)
- **앱 삭제 전 등록 해제 안내**를 전 서피스에 배치했습니다(위험 작업 구역·자동 등록 중단
  서브텍스트, 음식점 안내서 콜아웃·FAQ, 기관 안내서, 홈페이지 FAQ). 해제 없이 앱을 지우면
  서버에 고아 등록이 남아 재설치 시 등록이 401로 막힙니다.

## [1.0.0-beta.40] - 2026-08-11

### 변경 (Changed)
- 등록 메뉴 재배치: 「신청 확인하기」→「온라인 자동 등록」(0건이면 흐림·탭 가능, N건이면 활성+배지),
  전달파일·QR을 수동 등록 카드 하단으로 이동, 카드 제목을 '수동 등록'으로.
- 담당자 웹 4단계에서 하단 설명 박스를 삭제하고 총 합계 한 줄로 간결화했습니다.

### 추가 (Added)
- 담당자 웹 **CSV 연속 업로드**: 추가/새로 시작을 고를 수 있고, 양식 예시를 다중 음식점으로 교체.

### 수정 (Fixed)
- 깨진 파일을 올리면 기존 명단이 날아가던 결함, 같은 파일 재선택 시 무반응 수정.

## [1.0.0-beta.39] - 2026-08-09

### 변경 (Changed)
- 검색 0건 안내문을 **지역 입력 상태를 인지하는 문구**로 교체하고, 해석 오류를 영속 표시합니다.

## [1.0.0-beta.38] - 2026-08-09

### 변경 (Changed)
- **동네 검색 전면 자동화**: 전 구역을 끝까지 순회하며 진행률·남은 시간을 보여주고,
  [그만 찾기]로 중단할 수 있습니다.

## [1.0.0-beta.37] - 2026-08-09

### 변경 (Changed)
- 검색 우선순위 재배치 — **지역(구·군+동)이 기본**이고 자동으로 동네를 순회하며,
  우편번호는 보조 수단으로 내렸습니다.

## [1.0.0-beta.36] - 2026-08-09

### 추가 (Added)
- **동 이름 검색(B안)**: 동→우편번호 정적 매핑(`zipmap/`, 전국 생성 파이프라인 포함)을 순회하는
  보조 경로와 전용 하니스 14검사를 추가했습니다.

## [1.0.0-beta.35] - 2026-08-09

### 추가 (Added)
- 네이버 지도 링크를 3개 서피스(앱·담당자 웹·안내서)에 배치했습니다.
- 동 이름 검색 A안 검증용 `server/probe-emd.mjs`(인증키 대화식 입력 또는 `server/.public-api-key`).

## [1.0.0-beta.34] - 2026-08-08

data.go.kr 일반음식점 API가 **조건값에 한글이 들어가면 무조건 0건**을 돌려주는 회귀 상태가 되어
(resultCode 정상 + totalCount 0) 설치 온보딩의 "우리 가게 찾기"가 전국에서 막혔습니다.
같은 조건에 ASCII를 넣으면 정상 동작하므로 우리 쪽 인코딩·키·쿼터 문제가 아닙니다(PROTOCOL §7.4).

### 추가 (Added)
- 서버 `/api/restaurants`에 **`zip`(도로명 우편번호 5자리)** 경로 신설 — 우편번호로 후보를 받아
  (최대 3페이지) 상호는 서버가 정규화 부분일치로 거릅니다. 상호 조건은 요청에 그대로 남겨 두어
  상대 서비스가 복구되면 원래 경로가 자동으로 살아납니다.
- 앱·담당자 웹의 가게 찾기에 우편번호 칸과 안내를 추가했습니다.

### 변경 (Changed)
- 가게 실존 대조를 이름 검색에서 관리번호 `cond[MNG_NO::EQ]`(ASCII) 정확 조회로 교체 —
  장애 중에도 가게 선점 방어가 동작합니다.

### 수정 (Fixed)
- 전화번호 매핑 오류(`TELNO`) 수정 — 그동안 항상 빈 값이었습니다. 응답에 `zip` 추가.

## [1.0.0-beta.33] - 2026-08-08

업무 프로세스 정정: 돈은 **음식점별로 각각 결제**됩니다(카드결제 다수, 계좌이체도 있음).
"총액 1건 이체" 전제로 만들어진 게이트·문구를 전수 정정했습니다.

### 변경 (Changed)
- 미등록 음식점 하드 차단 → 제외 목록 + 1회 확인(유사 이름 제안·미등록/타관할 구분).
- 입금자명 필수 강제 제거(PROTOCOL에서 원래 선택 필드), 4단계를 가게별 합계·소계로 재구성.
- 「통장 확인 후 승인」→「결제 확인 후 승인」, 확인창을 금액·기관·결제자·인증 도메인·전송 시각
  순으로 재구성하고 확인 경로를 카드/계좌이체 병기로.

### 추가 (Added)
- **인증 도메인 표시(F-04)**: 토큰의 이메일 도메인만 저장·전달하며(로컬파트 없음, body 위조 무시)
  앱 수신함·확인창에 표시합니다. `null`은 "확인 불가".
- 결제구분(선택) CSV 열 → `items.payMethod`, 앱이 방식별 안내를 분기합니다. `batch_hash` canonical 불변.
- inbox summary에 `created_at`(전송 시각) 추가.

## [1.0.0-beta.32] - 2026-08-06

### 변경 (Changed)
- 승인·직접 전달 확인창을 **입금 대조 중심**으로 재편했습니다(입금자명·총액·이름 단위 반영 내역을
  한 창에서 확인 후 등록 — 원칙: 입금 1건 ↔ 명단 1건).
- 경로 공통 중복 검사(`receivedBatchLog`)를 서버 승인·직접 전달 양쪽에 적용하고, 중복은
  **하드 차단 대신 시점을 실은 경고 후 사장님 판단**으로 바꿨습니다(다음 달 동일 명단 오차단 해소).

### 추가 (Added)
- `items[].payer`(입금자명) — 암호문 내에서만 이동하며 `batch_hash` 불변, 거래 `note`에 기록됩니다.
- 담당자 웹 인증 대기 화면이 **보낸 주소를 되짚고** [주소 고쳐 다시 받기] 경로를 제공합니다
  (주소 오타 시 반송은 발신자에게만 남아 담당자가 무한정 기다리던 현장 문제).

## [1.0.0-beta.31] - 2026-08-06

OWASP·STRIDE·NIST 기준 4개 독립 조사를 종합한 보안 점검(🔴6 / 🟡12 / 🟢13)의 조치를 일괄 반영했습니다.
보고서는 `docs/handover/11-security-review-2026-08.md`에 있습니다.

### 수정 (Fixed)
- **XSS 3종 차단**: CSV 오류 메시지(정상 업무 경로), 서명 dataURL 3곳, 복원 meta 화이트리스트.
- 무결성 대조가 항등식이라 아무것도 잡지 못하던 문제를 수정하고, **해시체인 v2**(종류·서명·사유 포함)
  + 꼬리 앵커로 꼬리 삭제·유형 변조·서명 교체를 탐지합니다.

### 추가 (Added)
- **가게 선점 방어**: 등록 시 공공데이터 실존·상호 대조(`verified`), 열쇠 지문(`ABCD-EF12`) 표시,
  담당자 웹 지문 확인 게이트(기관+부서 단위 1회, 서버 기록), 신규 등록 배지, 선점 신고 경로.
- 수신함 소유증명 필수화(+ `batch_hash` 응답 제거), 총액·인원 대조 검증.
- CSP meta·`_headers` 신설, 보안 응답 헤더 4종, OTP 일일 예산·IP 한도, 챌린지 상한,
  feedback 전화번호 차단, 이메일 pepper(HMAC).
- `__diag_` 접두 진단 id — 실존 대조 면제 + 담당자 목록 제외(실제 가게 id로 스모크를 돌리는 것
  자체가 선점이므로).

### 변경 (Changed)
- PIN을 **PBKDF2 600k**로 전환(자동 업그레이드), djb2 폴백 제거·비보안 컨텍스트 차단,
  SW 교차출처·오류 응답 캐시 차단, 잠금 중 자동 백업 금지, 디버그 훅 노출 제한.
- 담당자 웹: 서버 주소 고정, 로그아웃·30분 자동 해제, `esc()` 홑따옴표, NUL 바이트 제거.

## [1.0.0-beta.30] - 2026-08-05

### 변경 (Changed)
- 부서 목록 관리를 직원 목록 아래로 이동하고, 하위 카드 제목을 15% 축소해 위계를 세웠습니다.

## [1.0.0-beta.29] - 2026-08-05

### 변경 (Changed)
- **설정 전면 1단 아코디언화**: 자동/수동 등록·직원 목록까지 접힘 카드로, 섹션 헤더를 3그룹
  (등록 / 장부 지키기 / 가게 기본설정)으로. 접힘 헤더에 `📩 신청 N건`·`⚠️ 등록 필요` 배지.
- **홈 상단 개편**: [장부 저장] → 30일 리마인더 배너, [직원 목록 관리] 바로가기(설정 카드 자동
  펼침 점프), [검색 초기화] → 검색창 옆 ✕(조건부).

## [1.0.0-beta.28] - 2026-08-04

### 수정 (Fixed)
- **재설치 등록 충돌 해소**: 완전 초기화가 열쇠 소멸 **전에** 서버 등록·클라우드 백업을 해제합니다
  (`wipeDeregisterGate`). PIN 분실 복구 경로는 의도적으로 미해제입니다(가게 가로채기·백업 소실 방지).
- `register-key`의 401 `auth_required`에 사장님 언어로 된 해결 안내(예전 폰 해제·열쇠 복원·문의)를
  붙이고, "인터넷 확인" 오답 토스트가 안내를 덮지 않도록 분기했습니다.

### 추가 (Added)
- 운영자 런북 §12: 고아 등록 수동 해제 절차(본인 확인·SQL·금지사항).

## [1.0.0-beta.27] - 2026-08-04

### 추가 (Added)
- 앱: 증표 사진 안내, 첫 실행 환영 화면, 서명 캔버스 안내(형제 오버레이 — dataURL 불변 증명),
  설정 아코디언, 🧾 증표 라벨.
- **`docs/handover/` 11종 신설**(MECE): 개요·아키텍처·데이터모델·계약 C1~C8·불변식·테스트 공백
  G1~G9·배포 런북·프런트 규약·미결·코드 재점검 R1~R23.

### 변경 (Changed)
- OTP 허용 도메인에 `or.kr`·`ac.kr` 추가(라벨 경계 강제로 우회 차단).
- `package.json` 라이선스 표기 일치, 직원 안내서 라벨을 새 아이콘 UI와 정합.

## [1.0.0-beta.26] - 2026-08-03

### 변경 (Changed)
- **온보딩 4→3단계**: 1/3에서 가게 검색으로 선택하면 이름·주소·전화가 자동으로 채워지고,
  중복 등록 단계가 사라집니다.
- 서버 `registered-list` 지역 매칭을 정규화 후 정확 일치로(부산 서구↔강서구 등 오매칭 3쌍 차단),
  검색 응답에 `tel` 필드 추가.

### 수정 (Fixed)
- **관할 목록 미표시 근본 수정**: 검색으로 고른 공식 주소를 `meta.storeAddr`에 보존해 `district`를
  전송하고, 부팅 시 1회 자동 치유합니다(라이브 D1의 `district`가 전국 0건이었습니다).

### 추가 (Added)
- 담당자 웹 CSV에 선택 열 **'소속'** 지원(헤더 3종 호환, 빈 값 자동 채움, `batch_hash` canonical 불변),
  소속만 다른 동일 명단의 충돌을 사전 차단.

## [1.0.0-beta.25] - 2026-08-03

### 변경 (Changed)
- **손님 흐름 4단계 직행**: [맞아요] 직후 바로 금액·서명 화면으로 가고(중간 잔액 화면 제거),
  잔액은 상단에 표시합니다. 최근 내역·사장님 부르기는 보조 링크로 내렸습니다.
- 소속 추천 칩을 한 명씩 등록·빠른 등록 양쪽에 노출(폰에서 `datalist` 팝업이 열리지 않던 것이 원인).

### 수정 (Fixed)
- 상세 화면 버튼 3개가 폰에서 접힘 아래로 밀려 초안으로 돌아갈 수 없던 문제, 초안 보유 시 유휴가
  30초로 줄어 금액이 사라지던 문제, [사장님 부르기]의 무경고 초안 폐기, 오류 표시 시 여유 부족.

## [1.0.0-beta.23] - 2026-08-02

### 변경 (Changed)
- **손님 요청 화면 통합**: 금액·서명·버튼을 한 화면에 담아, 폰에서 버튼이 화면 밖 174px에 있던 것을
  전 기종 화면 안으로 압축했습니다(서명판 svh 비례, 태블릿은 더 크게).
- 손님 검색을 **접두 일치**로(초성도 접두). 사장님 홈 검색은 부분 일치를 유지합니다.

### 수정 (Fixed)
- 캔버스 3중 보호(부분 갱신·게이트·render 강등)로 금액 타이핑·토스트·네트워크 전환·회전에도
  서명이 보존됩니다. 회전 시 서명 잉크가 잘리거나 압축되던 문제 수정.
- 유휴 시계를 마지막 사용자 입력 기준으로 바꿔 화면이 굳지 않도록 했습니다.
- 렌더 예외 10건 → 0건 근본 수정, 하니스가 `consoleProblems`를 검사하도록 봉인.

## [1.0.0-beta.21] - 2026-08-02

### 추가 (Added)
- **B단계 손님 사용 요청**: 손님이 [사용 요청하기]에서 금액(잔액 초과 불가)과 서명을 작성하고
  [사장님 확인 받기]를 누르면, 사장님이 "사장님 확인" 화면에서 금액·서명을 보고 PIN 해제 후
  프리필된 모달에서 확인·수정·저장합니다(자동 저장 없음).
- 요청은 **휘발성**입니다 — 원장·백업·서버에 흔적 0, 2분 TTL. 금액을 요청과 다르게 고치면
  서명이 자동 무효화되고 재서명을 받습니다(`note`에 요청 금액 기록).

### 수정 (Fixed)
- 고친 금액이 [서명 다시 받기]·네트워크 전환으로 되돌아가던 치명 결함 2건, 토스트가 서명·입력을
  지우던 버그, 서명 중 렌더 방어, 모달 초기 포커스, `parseWon` 표기 검증.

## [1.0.0-beta.20] - 2026-08-02

### 수정 (Fixed)
- **손님 화면 모바일 입력 오류**: 글자마다 전체 재렌더로 입력 노드가 교체되어, 한글 IME 한 키 안의
  `compositionend`→`start` 사이에 노드가 사라지면서 글자가 표시되지 않고 폰 키보드가 닫혔습니다
  (데스크톱 무증상). `custSearchPatch()`로 `#custResults`만 교체해 입력 노드·포커스·조합 상태를
  유지합니다.

### 추가 (Added)
- 검색 0건 안내("'○○'(으)로 등록된 이름이 없어요")와 초성 팁 — 질의 전에는 표시하지 않습니다.

## [1.0.0-beta.19] - 2026-08-02

### 변경 (Changed)
- 손님 인계 시 별도 안내 화면을 없애고 바로 PIN 화면으로(대상 이름·소속 표시), [처음으로]로 요청 취소.
- 홈 [손님에게 넘기기] → [손님 셀프 조회].
- 유휴 시계 3종 정리: 손님 30초 / 사장님 확인 30초 / 사장님이 직접 연 PIN 화면 120초
  (만료 시 입력 PIN 폐기·손님 화면 복귀).

### 수정 (Fixed)
- 부분 입력 PIN 무기한 잔류(해제 난이도 저하), 손님 오탭 시 PIN 패드 영구 체류,
  lock-to-pin의 pending 잔류, 만료된 요청을 계속 광고하던 문제.

## [1.0.0-beta.18] - 2026-08-01

### 추가 (Added)
- **손님 셀프 조회**: 이름(초성) 검색 → 본인 확인 → 잔액 + 최근 5건 → [사장님께 보여주기],
  30초 자동 복귀. 잔액 목록은 표시하지 않고 동명이인 선택은 차단합니다.
- 사장님: 90초 자동 잠금 / [손님에게 넘기기], PIN 해제 시 2분 내 pending이면 사용 모달 자동 오픈
  (금액 프리필 없음).
- 잠금 중 allowlist 게이트(파일 input 포함), 복구 화면 파괴 버튼 60초 카운트다운,
  PIN 실패·지연 상태를 meta에 영속화.

### 수정 (Fixed)
- **원장 체인 오판 근본 수정**: 배치 `createdAt`의 무작위 소수를 제거(`nextTxTime` 단조)하고
  `verifyChain`을 링크 추적 방식으로 재작성(레거시 호환) — 손님 잔액이 간헐적으로 실패하던 문제 제거.
- 동명이인 마커 `a/b` → `·2`(라틴 이름이 정상 표기되도록).

## [1.0.0-beta.17] - 2026-08-01

### 추가 (Added)
- **초성 검색**(ㄱㅁ)·공백 무시, 사용 모달의 9,000/18,000/27,000 빠른 금액, 검색 1건 확대 카드
  (요약 카드 동시 숨김 — 손님 앞에서 매출 비노출), 검색 중 최근 사용 순 정렬.

### 변경 (Changed)
- 월간 백업 배너 → 상단 [장부 저장] 상시 버튼(기한이 되면 ⚠️ 배지), '전체 부서' → '전체 소속'.
- 검색창 전폭 2행 상단바. 사장님 개입이 17탭에서 약 8탭으로 줄었습니다.

## [1.0.0-beta.16] - 2026-07-31

### 변경 (Changed)
- 정렬 통합 — 공공기관 최우선, 나머지(회사·개인·무소속)는 하나의 가나다 블록으로.
  수신함 승인 전 표기도 결합 라벨로 통일했습니다.

## [1.0.0-beta.15] - 2026-07-30

### 변경 (Changed)
- 그룹 제목·정렬 확정 — 공공기관은 "기관명 부서명"을 항상 표시하고, 기관 우선·가나다·부서
  오름차순을 홈과 설정에서 공통으로 적용합니다.

## [1.0.0-beta.14] - 2026-07-30

### 추가 (Added)
- **홈 계층 그룹핑**: 공공기관=부서명 / 회사=소속명+부서 하위 / 개인 그룹, 아코디언(탭·키보드
  펼침, 인원·소계 헤더, 검색·필터 시 자동 펼침)과 하단 요약 → 설정 직원 목록 관리로 이동.

### 변경 (Changed)
- `orgKind` 플래그와 라벨 불변 백필(동일 라벨 전체), 수정 시 분류 재설정, 그룹 키 JSON화,
  필터 자기치유.

## [1.0.0-beta.13] - 2026-07-30

### 추가 (Added)
- **직원 소속(org) 필드**: 자동 등록에서 분리 저장하되 표시는 결합 라벨로 유지하고, 매칭 기준은
  "화면에서 같으면 같은 직원"입니다. 승인·직접 전달 시 기존 직원은 충전으로 처리해 월 재전송
  카드 증식을 막습니다.
- 폰 반응형 회귀 하니스 `harness/responsive.e2e.mjs` 신설(108검사) — `verify-all` 10항목으로 확장.

### 변경 (Changed)
- 앱 전반의 '기관' → **'공공기관'** 전수 교체, 공공기관 부서 목록 관리, 워크플로 문서화
  (공공기관=자동 / 회사·개인=수동).
- `.stack` minmax + 직원 카드 2행 등 폰 반응형 정리, UX 16건 개선(대비·배지·빈 상태 CTA·
  폰 모달 버튼 순서·해요체 통일 등).

### 수정 (Fixed)
- CSV '소속부서' 헤더 충돌, 내보내기 잔액표에 홈 필터가 반영되지 않던 문제.

## [1.0.0-beta.12] - 2026-07-27

### 변경 (Changed)
- **용어 대개편**: 상위 개념을 "직원 선금대장 등록"으로 두고 **자동 등록**(기관에서 보낸 명단 받기) /
  **수동 등록**(직접 입력)으로 정리했습니다. 설정을 자동/수동 등록 카드로 재배치하고, 온보딩을
  4단계(우리 가게 등록 필수 유도 + 상시 탈출구)로, 열쇠 백업을 강권합니다. `manual.html` 삭제.
- 담당자 웹: 동명 음식점 오배송 3중 차단, 전체선택·진행률·백오프·결과표·재시도,
  토큰 `sessionStorage` 보존(새로고침마다 OTP 소모 방지), CSV 오류 '사유' 열, 에러 한글화.

### 추가 (Added)
- `📩 새 명단 알림` 칩과 서버 `/api/inbox-count`.
- 안내서 3종을 실구현과 일치시키고 A4 인쇄 CSS + PDF 3종(`docs/manuals/`) 생성.
- 홈페이지 3주체(사장님·서무·직원) 재구성, 직원 안내서(`docs/manual-staff.html`) 신설,
  사용 흐름 5단계 인포그래픽, 전용 태블릿 비치 안내.

### 보안 (Security)
- 담당자 이메일을 해시로만 저장(불변식 3), `submit` 멱등 응답 `{deduped,status}`,
  OTP 스로틀·형식 보강, feedback 180일 TTL, 크론 배치화.
- Resend 429를 `429 {error:'email_quota_exceeded'}`로 분리해 담당자 웹이 발송 한도를 안내합니다.

## [1.0.0-beta.10] - 2026-07-01

### 추가 (Added)
- **명단 제외를 당사자가 처리**하도록 정리했습니다(운영자는 결정자가 아니라 신호 제공자):
  음식점의 「선금 받기 중단」(공개키 삭제로 신규 수신만 차단, 원장·잔액은 유지, `POST /api/deregister`),
  담당자 웹의 '선금 받기 가능/미등록' 표시(`GET /api/registered`)와 기관별 로컬 제외 목록.

### 변경 (Changed)
- 설정 정리: '다자간 확장(베타)' 카드 해체, 공개키 보기 제거(자동 등록으로 불필요),
  개인키 백업을 「기관 단체 선금 받기」 카드로 이동, 나머지를 「장부 도구」로 개칭.
  백업 배너를 홈·이력에서 설정 화면 상단으로 이동.

## [1.0.0-beta.9] - 2026-07-01

### 변경 (Changed)
- **음식점 주인 관점의 서버 연동 UX 정리**: 서버 주소를 앱에 내장(`DEFAULT_RELAY`)해 매번 입력할
  필요를 없애고, 기술 용어를 걷어냈습니다("서버 연동/공개키 등록/음식점 ID" → "기관 단체 선금 받기",
  "우리 가게 등록하기", "기관에서 온 신청 확인").
- 「우리 가게 찾기」 모달에서 이름(+선택 지역코드)으로 검색·선택하면 자동 등록됩니다.
- 서버 `/api/restaurants`: 지역·이름 중 하나만 있어도 검색되고 폐업 업소는 제외됩니다.

### 추가 (Added)
- data.go.kr 행정안전부 일반음식점 조회서비스 연동 확정(`/info`, `cond[...]` 조건,
  `returnType=json`, `numOfRows` 최대 100)과 실응답 기준 필드 매핑.
- `harness/phase2.live.mjs`(실배포 서버 대상 라이브 스모크), `harness/verify-all.sh`(원커맨드 재점검),
  `server/test-key.mjs`(공공 API 키 한 줄 테스트).

## [1.0.0-beta.8] - 2026-07-01

다자간 확장 2차: 기관 담당자–음식점 서버 연동(중계 서버·담당자 웹)을 추가했습니다.

### 추가 (Added)
- **중계 서버**(`server/`, Cloudflare Worker + D1): 공개키 등록, LOCALDATA 일반음식점 검색 프록시(지역 필수), 암호문 blob 중계, 부서별 총액·해시(`deposit_summary`) 저장. 서버는 평문 개인정보를 저장·로깅하지 않으며(불변식) 평문 ciphertext 제출을 거부합니다.
- **담당자 웹**(`agency-web/`): 기관 이메일 확인, 음식점 검색, CSV 검증(차단/경고), 제3자 제공 동의, RSA-OAEP+AES-GCM 하이브리드 암호화 후 전송.
- **음식점 앱 서버 연동**: 설정 「서버 연동」에 공개키 등록 + 기관 신청 수신함. 승인 시에만 명단을 복호화하고 `batch_hash`로 전송 변조를 대조한 뒤 동명이인 보완·원장 등록. 서버 주소 미설정 시 기존 오프라인 동작은 그대로입니다.
- 프로토콜 문서(`server/PROTOCOL.md`)와 로컬 개발 서버(`server/dev-server.mjs`), 엔드투엔드 검증(`harness/phase2.e2e.mjs`). ※ 당시 함께 만든 배포 문서 `docs/phase2-deploy.md`는 배포 내부 정보라 이후 리포에서 제외됐습니다(로컬 보관) — 배포 절차는 [`docs/handover/07-deploy-runbook.md`](docs/handover/07-deploy-runbook.md)를 보십시오.

## [1.0.0-beta.7] - 2026-06-30

다자간 확장(기관–음식점–직원) 1차: 음식점 앱(원장) 로컬 기반을 추가했습니다. 서버 연동(담당자 웹·중계 서버)은 2차로 분리합니다.

### 추가 (Added)
- 거래 무결성 해시 체인(`tx_hash`/`prev_hash`)을 도입하고, 설정의 `무결성 검증`에서 체인 재계산과 입금·사용·잔액 합계 대조를 제공합니다. (스펙 §4.3, §7)
- RSA-OAEP 2048 키페어를 최초 실행 시 생성하고, 개인키는 AES-GCM으로 래핑해 기기에 저장합니다. (스펙 §4.1)
- `개인키 백업`: 사용자 암호로 보호된 개인키 백업 파일을 저장·복원합니다(기기 분실 대비). (스펙 STEP 12)
- `잔액 문자 안내(SMS 옵션)`: 차감 후 기기 문자앱으로 잔액을 안내합니다(직원명 미포함). 번호는 직원 동의 시에만 암호화 저장하며 `전화번호 일괄 파기`로 정산 종료 시 삭제합니다. (스펙 STEP 10, §5.2)
- CSV 임포트에 동명이인 자동 보완(`a/b/c` 접미사)과 경고를 추가했습니다. (스펙 §2.4)

### 변경 (Changed)
- IndexedDB 스키마를 v3로 올렸습니다(비파괴 마이그레이션: 기존 직원·거래 무손실). 백업 형식도 v3로 올렸으며 v1/v2 복원과 호환됩니다.

## [1.0.0-beta.6] - 2026-06-17

Android PWA 설치 안정성과 데이터 삭제 위험 안내를 보강했습니다.

### 추가 (Added)
- Android 설치 안정성을 높이기 위해 `icons/icon-192.png`, `icons/icon-512.png` PNG 아이콘을 추가하고 manifest에 등록했습니다.
- 오프라인 캐시에 PNG 아이콘을 포함했습니다.
- Chrome 사이트 데이터, 캐시, 저장공간 삭제 전 `장부 안전 저장`을 해야 한다는 안내를 앱 화면과 매뉴얼에 추가했습니다.

## [1.0.0-beta.5] - 2026-06-16

설정 화면의 데이터 관리와 부서 관리 흐름을 정리했습니다.

### 변경 (Changed)
- 설정 화면의 중복 `전체 초기화` 구역을 제거하고 데이터 관리 안에 한 번만 표시되도록 정리했습니다.
- `장부 엑셀로 저장`과 `데이터 안전 저장`을 `장부 안전 저장` 버튼 하나로 통합했습니다.
- 기관 부서 불러오기에서 기관을 바꾸면 현재 기관명이 자동으로 갱신되도록 했습니다.
- 거래 이력 삭제 위험이 큰 `새 달 시작` 기능을 설정 화면에서 제거했습니다.
- 설정 화면 메뉴 순서를 가게, 부서, 직원, 데이터, 보안 순으로 재배치했습니다.

## [1.0.0-beta.4] - 2026-06-16

서울시청 및 서울시 25개 구청의 과 단위 부서 선택 기능을 추가했습니다.

### 추가 (Added)
- `agency-departments.json`에 기관별 과 목록을 분리해 향후 데이터 업데이트를 쉽게 했습니다.
- 초기 설정과 설정 화면에서 기관을 선택하고 필요한 과를 복수 선택할 수 있습니다.
- 앱 안에서 기관 부서 목록을 새로고침할 수 있습니다.

## [1.0.0-beta.3] - 2026-06-16

소규모 음식점 로컬 사용에 맞춘 배포 준비 버전입니다.

### 변경 (Changed)
- PIN 5회 실패 후 PIN만 삭제하던 흐름을 제거하고, 모든 로컬 데이터를 삭제하는 앱 초기화만 허용하도록 변경했습니다.
- 백업 포맷을 `schemaVersion: 2`와 `payload` 구조로 정리하고, 복원 전 데이터 검증을 추가했습니다.
- 초기 설정의 외부 가게 검색 기능을 제거하고 음식점 정보 직접 입력만 남겼습니다.
- 설정 화면에 `새 달 시작` 버튼을 노출했습니다.
- 취소된 사용 거래는 일자별 사용 합계에서 제외합니다.
- 기본 기관명과 부서 예시를 특정 기관이 아닌 일반 매장용으로 변경했습니다.

## [1.0.0-beta.2] - 2026-06-04

내부 베타 기능 개선 버전입니다.

### 추가 (Added)
- **AI 음성 검색(STT)**: 홈 화면 직원명 검색에 음성 인식
  (Web Speech API, 한국어) 버튼(🎤)을 추가했습니다. 어르신·바쁜 매장에서 손을 쓰지
  않고 이름을 말해 검색할 수 있습니다.

## [1.0.0-beta.1] - 2026-06-04

첫 베타 테스트 공개 버전입니다. (이전 `2.x` 표기는 내부 개발 과정의 번호로,
첫 외부 공개에 맞춰 `1.0.0-beta.1`로 재정비했습니다.)

### 주요 기능 (Features)
- 부서별 직원 선입금 잔액 관리
- 식사 사용 시 서명 기반 차감
- 일자별/직원별 거래 이력 조회
- 데이터 백업/복원 (JSON)
- 장부 엑셀(CSV) 내보내기
- 4자리 PIN 잠금
- 초기 설정 마법사
- 오프라인 지원 (서비스 워커 캐시)

### 안정성 개선 (Fixed / Changed)
- **복원 버그 수정**: 안드로이드 브리지 복원 콜백 `onRestoreData`가 JSON만 파싱하고
  실제 복원을 수행하지 않던 빈 스텁 문제를 해결했습니다. 파일 복원과 동일한 로직을
  `restoreFromParsed`로 공통화하여 두 경로가 일관되게 동작합니다.
- **데이터 보존**: 복원 데이터에 직원/거래가 있으나 백업 메타에 `setupComplete`가
  없을 때, 복원 후 초기 설정 마법사로 되돌아가 데이터에 접근하지 못하던 문제를
  방지하도록 `setupComplete`를 유지합니다.
- **초기 설정 폴백**: 초기 설정 중 가게 정보를 직접 입력해 가게 등록을 계속 진행할 수 있습니다.
- **캐시 동기화**: 서비스 워커 캐시 이름을 앱 버전과 일치시켜 신버전이 정상 배포되도록
  하고, `manual.html`을 캐시 자산에 추가했습니다.
- **당겨서 새로고침 방지**: 안드로이드에서 화면을 아래로 당겼을 때 페이지가 새로고침되어
  비밀번호 화면으로 되돌아가던 현상을 막기 위해 `overscroll-behavior:none`을 적용했습니다.
  (앱 재시작 시 비밀번호를 다시 묻는 보안 동작 자체는 유지됩니다.)
- **최신 버전 보장**: 서비스 워커를 화면(HTML) 요청에 한해 네트워크 우선으로 변경하여,
  온라인 상태에서는 항상 최신 버전을 받고 오프라인일 때만 캐시로 폴백하도록 했습니다.
- **배포 자동화**: `GitHub Actions` 기반 Pages 배포 워크플로를 추가하여 `main` 변경 시
  자동으로 최신 버전이 게시되도록 했습니다.
